[source.crates-io]
# Use crates.io directly during CI builds. Vendor directory may be
# incomplete in some environments.

# vendor support remains optional; path retained for local offline builds
[source.vendored-sources]
directory = "vendor"

[net]
offline = true
