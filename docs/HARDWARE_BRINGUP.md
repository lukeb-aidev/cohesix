<!-- Copyright 2026 Lukas Bower -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Purpose: Document Cohesix Raspberry Pi 4 hardware bring-up workflows and constraints using the official U-Boot + binary image path. -->
<!-- Author: Lukas Bower -->
# Hardware Bring-up (As-Built)

## Scope
- Cohesix supports two bring-up paths:
- QEMU `aarch64/virt` (development/CI baseline).
- Raspberry Pi 4 (`bcm2711`) via upstream-style boot chain: `Pi firmware -> U-Boot -> seL4 image -> root-task`.
- Milestone 26 defines the strict no-NIC baseline on Pi 4; Milestone 26a adds profile-gated GENETv5 + static IPv4; the current 26b as-built state adds the interactive U-Boot policy wizard, DTB `/chosen/cohesix,*` handoff, bootloader-exported xHCI BAR handoff for local-seat, wired DHCP, and the staged Pi 4 Wi-Fi runtime path.

## Canonical Pi 4 boot chain
1. Pi boot firmware loads `start4.elf` and `fixup4.dat`.
2. Firmware loads `u-boot.bin` (from FAT boot partition).
3. U-Boot loads the staged Cohesix payload (`cohesix-image-arm-bcm2711`) and the padded staged Pi 4 DTB (`bcm2711-rpi-4-b.dtb`) using `fatload`.
4. U-Boot applies any saved/runtime Cohesix policy into DTB `/chosen/cohesix,*`, quiesces the USB host stack, and transfers control with `bootm`.
5. seL4 enters root-task; Cohesix reaches `cohesix>` prompt.

## Manifest profiles
- Development profile: `configs/root_task.toml` (`profile.name = "virt-aarch64"`).
- Pi 4 baseline profile (Milestone 26 no-NIC): `configs/root_task_uefi_aarch64.toml` (`profile.name = "uefi-aarch64"` migration alias).
- Pi 4 networking profile (Milestone 26a/26b policy baseline): `configs/root_task_pi4_uboot_aarch64.toml` (`profile.name = "pi4-uboot-aarch64"`).
- `coh-rtc` enforces profile gates:
- Milestone 26 baseline: `hw.no_nic=true`, `features.net_console=false`.
- Milestone 26a/26b networking: `hw.network.enabled=true`, `hw.network.backend=bcmgenet-v5`, bounded `hw.network.mode` (`off|static|dhcp`), bounded `hw.network.interface` (`wired|wifi|auto`), DHCP retry/timeout bounds, required `net` device declaration, and bounded non-zero static IPv4 (`prefix_len=1..32`) when `mode=static`.
- declared `uart` + `rtc` devices
- local-seat declarations when `hw.local_seat.enabled=true`
- attestation policy/device requirements when `hw.attestation.enabled=true`

## Pi 4 build + boot commands
1. Build seL4 Pi 4 image:
`cmake --build seL4/build_UEFI --target images/sel4test-driver-image-arm-bcm2711`
2. Build U-Boot (Pi 4):
`make -C third_party/u-boot rpi_4_defconfig`
`make -C third_party/u-boot CROSS_COMPILE=aarch64-linux-gnu- -j$(sysctl -n hw.ncpu)`
3. For a full clean Pi 4 rebuild, use:
`scripts/pi4-image-build.sh --clean --manifest configs/root_task_pi4_uboot_aarch64.toml`
- `--clean` removes `root-task` Cargo artifacts, cleans and rebuilds the Pi 4 seL4 U-Boot build tree, and runs a fresh `rpi_4_defconfig` U-Boot rebuild before staging/flashing.
- On macOS ARM64, `--clean` also injects the Homebrew OpenSSL include/lib paths into the U-Boot host-tool build so `mkimage` and the other host utilities rebuild against the expected OpenSSL installation.
4. Prepare FAT boot partition with:
- Pi firmware files (`start4.elf`, `fixup4.dat`, required DTB/overlay files),
- `u-boot.bin`,
- `cohesix-image-arm-bcm2711` (copied from seL4 `images/sel4test-driver-image-arm-bcm2711`),
- Cohesix manifest artifacts (`manifest.json`, `manifest.sha256`, and related packaged assets).
5. Ensure `config.txt` includes:
- `arm_64bit=1`
- `enable_uart=1`
- `kernel=u-boot.bin`
- `dtoverlay=upstream-pi4`
- `core_freq=250` (keeps UART handoff deterministic during firmware -> U-Boot transition)
6. Boot from U-Boot shell:
- `fatls mmc 0:1`
- `fatload mmc 0:1 ${loadaddr} cohesix-image-arm-bcm2711`
- `go ${loadaddr}`

## U-Boot networking setup (for 26a/26b prep)
- Milestone 26a uses static IPv4 in runtime. The current 26b Pi 4 path adds an interactive U-Boot wizard plus persisted `cohesix.env` policy handoff through a staged padded DTB and U-Boot `bootm`, while keeping manifest defaults authoritative when the handoff is absent or invalid.
- Typical env setup:
- `setenv autoload no`
- `setenv ipaddr <board-ip>`
- `setenv serverip <host-ip>`
- `setenv ethact <interface>`
- The staged `boot.cmd` now presents a Linux-style numbered wizard over the existing U-Boot consoles:
- `Continue with existing config` is the default action when saved Cohesix policy already exists in `cohesix.env`.
- `Boot with manifest defaults` is the default action when no saved Cohesix overrides exist.
- `Configure networking` walks DHCP `ON|OFF`, `wired|wifi`, Wi-Fi credentials when needed, and static IPv4 prompts only when DHCP is off.
- `Save current settings and reboot` persists only the user-facing Cohesix policy fields to `cohesix.env` on the FAT boot partition; it does not rewrite the manifest or generic U-Boot environment.
- The wizard intentionally uses `askenv`-based numbered prompts instead of U-Boot `bootmenu`, because upstream U-Boot documents `bootmenu` as an ANSI-terminal path and the simpler prompt flow is more reliable on the Pi 4 HDMI + USB-keyboard bring-up path while still preserving serial control via `minicom`.
- The script reloads only `cohesix.env` from the FAT partition, keeps the PREBOOT console serial-first, bootstraps menu/input USB with `pci enum; usb start` only when the wizard actually needs an input session, and switches `stdin` to `usbkbd,serial` only after that session is live so the HDMI wizard prefers the USB keyboard while retaining serial fallback. The menu no longer emits the old bring-up-only USB diagnostic snapshots or the extra USB diagnostics submenu; that U-Boot chatter was removed once the runtime handoff path became the primary evidence source.
- The Pi 4 U-Boot build stays on the seL4-aligned `usbkbd` interrupt-polling baseline (`CONFIG_SYS_USB_EVENT_POLL=y`) so the HDMI wizard matches the previously working Pi 4 input path.
- Optional Cohesix net-policy overrides mirrored into DTB `/chosen` by the staged boot script:
- `setenv coh_net_mode <off|static|dhcp>`
- `setenv coh_net_interface <wired|wifi|auto>`
- `setenv coh_static_ip <ipv4>` (mirrored into DTB `/chosen/cohesix,static-ipv4`; only applied when the effective mode is `static`)
- `setenv coh_static_prefix_len <1..32>` (mirrored into DTB `/chosen/cohesix,static-prefix-len`; only applied when the effective mode is `static`)
- `setenv coh_static_gateway <ipv4>` (mirrored into DTB `/chosen/cohesix,static-gateway`; optional and only applied when the effective mode is `static`)
- `setenv coh_wifi_ssid <ssid>` (mirrored into DTB `/chosen/cohesix,wifi-ssid`; used by the CYW43455 path when `coh_net_interface=wifi` or when `auto` prefers Wi-Fi)
- `setenv coh_wifi_psk <psk>` (mirrored into DTB `/chosen/cohesix,wifi-psk`; used by the CYW43455 path for open/WPA2-PSK join)
- The staged boot script now hands the saved `coh_wifi_*` variables directly to `fdt set` without mutating or persisting escaped shadow copies, so repeated boots and policy-file writes do not grow backslashes or corrupt WPA2 credentials. If an older card reports `Wi-Fi credential handoff overflow`, clear the saved `coh_wifi_ssid` / `coh_wifi_psk` values once and re-enter them through the wizard.
- The staged `bcm2711-rpi-4-b.dtb` is padded to 128 KiB before flashing, so U-Boot can add `/chosen/cohesix,*` properties in place without `fdt resize`.
- `setenv coh_show_logo <0|1>` (controls whether the staged `boot.bmp` splash is displayed on HDMI before the menu)
- The staged Pi 4 U-Boot build enables 24bpp BMP drawing, so the centered `boot.bmp` splash uses the same HDMI framebuffer path as `bmp display`.
- Pi 4 U-Boot now keeps `CONFIG_PREBOOT` on the seL4-aligned serial/video console path only; the HDMI wizard owns the first `pci enum; usb start` so boot no longer pays an unconditional duplicate USB-host bring-up before the menu. The default Pi 4 path still avoids a global scripted USB startup delay, but it keeps a targeted Apple `05ac:1006` keyboard-hub quirk in the U-Boot hub driver: a 250 ms post-config settle and a 5 s hub debounce timeout before downstream child probing.
- On first entry to the root menu, the staged `boot.bmp` copy of the Cohesix logo is shown centered while the wizard prepares the menu/input USB session, and the interactive menu is then drawn on a cleared console. The default splash path no longer burns an extra fixed sleep before menu bring-up.
- `ping ${serverip}`
- `fatwrite mmc 0:1 ${coh_policy_addr} cohesix.env ${filesize}` (only when explicitly persisting Cohesix policy)
- The staged `boot.cmd` copies these env vars into `/chosen/cohesix,*` on the staged DTB before `bootm`. If that DTB cannot be loaded or updated, the script aborts before handoff instead of silently booting with stale policy. Saved Cohesix policy persists across reboots via `cohesix.env`, but the manifest remains the build-time default and is never rewritten on the SD card.

## Milestone 26a Pi 4 network checklist
1. Build/validate the QEMU U-Boot harness:
- `scripts/uboot/qemu-uboot-smoke.sh --net user`
2. Build Pi 4 payload with 26a manifest defaults:
- `scripts/pi4-image-build.sh --manifest configs/root_task_pi4_uboot_aarch64.toml`
3. Boot on Pi 4 and verify runtime lines include:
- `manifest.hw.network.enabled=true`
- `manifest.hw.network.backend=bcmgenet-v5`
- `manifest.hw.network.static_ipv4.ip=<configured-ip>`
- `manifest.hw.networking=enabled-static-ipv4`
4. Validate TCP console reachability from host:
- `cargo run -p cohsh --features tcp -- --transport tcp --host <STATIC_IP> --port 31337 --script scripts/cohsh/boot_v0.coh`

## Milestone 26b Pi 4 network wizard checklist (as-built)
1. Build Pi 4 payload with the 26b policy-capable manifest:
- `scripts/pi4-image-build.sh --manifest configs/root_task_pi4_uboot_aarch64.toml`
2. At the U-Boot wizard, either:
- wait for `Continue with existing config` or `Boot with manifest defaults`, or
- choose `Configure networking` and walk the prompts:
  - `DHCP ON (automatic address)` or `DHCP OFF (static IPv4)`
  - `Wired Ethernet (GENET)` or `Wi-Fi (CYW43455)`
  - when `DHCP OFF`, enter `Static IPv4 address`, `Prefix length`, and optional `Gateway IPv4`
- when `Wi-Fi`, enter `Wi-Fi SSID` and optional `Wi-Fi PSK`
- rely on the runtime `[local-seat] ...` xHCI handoff breadcrumbs for current Pi 4 keyboard evidence; the old U-Boot USB diagnostic submenu and snapshot spam were removed once the cold-start handoff path became the active debug surface
- when firmware hands VL805 to root-task in a halted interrupt-quiesced stop state, runtime USB init may now emit `fw-handoff-skip-reset` before the config/DCBAAP stages; that is the expected fast path for the trusted Pi 4 handoff and replaces the older forced second HCRST on this path
- if the trusted runtime handoff still freezes during the first controller ownership write, the exact-issue surface now reports `pre-run-config-store-wedged` at `xhci.diag stage=0x0238`, which means the remaining blocker is the `CONFIG` store itself rather than the later DCBAAP/CRCR/event-ring publishes
3. Optional persistence:
- choose `Save current settings and reboot` or `Save settings and reboot` to persist only the Cohesix policy fields into `cohesix.env`.
4. Boot and confirm policy evidence:
- `[net-policy] source=manifest ...`, `[net-policy] source=dtb ...`, or `[net-policy] source=dtb rejected reason=<reason> ...`
- `manifest.hw.network.mode=static`
- `manifest.hw.network.interface=wired`
- `manifest.hw.networking=enabled-static-ipv4`
- for the manifest-default boot, `[net-console] init: bringing up backend=... mode=static interface=wired ip=192.168.10.42/24 ...`
- for wizard-selected `mode=dhcp`, `[net-console] pending-dhcp ...` followed by `[dhcp] lease bound ...` and then `[net-console] ready ip=<lease-ip> ...`
- for wizard-selected `mode=static`, `[net-console] init: bringing up backend=... mode=static interface=... ip=<static-ip>/<prefix> ...`
- if the staged DTB carries an explicit invalid override such as `wifi-psk-too-short`, root-task now rejects that override, emits `[net-policy] source=dtb rejected reason=<reason> ...`, and disables net-console with `invalid net config: dtb override rejected (<reason>)` instead of silently falling back to the manifest-selected backend.
- when saved wizard settings are used after reboot, root-task must not print `[boot] dtb locate skipped/failed: bootinfo extra truncated`; that message indicates the DTB handoff was lost before policy resolution and is a regression.
- when net-console never comes online, the serial/local-seat `nettest` and `netstats` commands must no longer collapse that state to a bare `detail=net-disabled`; they now preserve the boot-time init detail as `detail=net-disabled cause=<net-console init detail>`, so a failed CYW43 bring-up can still be diagnosed after the shell is live.

### Minimal next Pi 4 capture
1. Boot normally and do not run any debug commands until the first `cohesix>` prompt appears.
2. At the prompt, run `wifi dump-state`.
3. Run `usb status`.
4. Run `usb probe-kbd`.
5. If Wi-Fi is still down, run `wifi retry` and then `wifi dump-state` once more.

Capture only these operator-facing lines from that session:
- `wifi: f2_state=... exact_error=... sdhci_read_diag=...`
- `wifi: verdict=... focus=... bootstrap=...`
- `usb: verdict=... focus=...`
- `usb: xhci stage=... tag=... exact=...`
- `usb: xhci values ...`
- any `[local-seat] xhci diag summary ...` line that appears during `usb probe-kbd`

- when Wi-Fi bring-up fails, the serial log must now identify the failing stage with `[cyw43] step: ...` and `[pi4-wifi] ...` breadcrumbs before the final `mailbox-*`, `sdio-*`, or `cyw43-*` error detail. In particular, firmware load now emits `firmware core-ctrl mode=cmd53-windowed-read32-cmd53-byte-current-window fallback=cmd53-byte-rewindow split-window` before the ARMCR4/SOCRAM AI-core sequence so bring-up logs prove the primary mixed CMD53 backplane control path and the staged CMD53 byte-mode recovery path that root-task is using. Control-plane reply diagnostics now also emit `control-plane snapshot ... f2-window=0x18000000 f2-port=0x08000 read_addr_mode=fixed write_addr_mode=fixed ...`, and the corresponding `linux-f2-read-shape` / `linux-f2-write-shape` breadcrumbs include that same port/window tuple so Pi 4 traces prove that Function 2 traffic is using the Linux chipcommon FIFO shape instead of the older raw-zero path. Boot-time bring-up now also avoids unconditional long reset-deassert and SDIO retry spins: SDHCI power-on first emits `settle stage=sdhci-power-on mode=poll ...` and only falls back to the older long settle when the controller never reports ready, reset deassert emits `reset state=deasserted action=logical-only settle=skipped`, and the optional pre-reset HT assist now keeps a shorter soft wait budget than the required final HT wait. The same path now also emits `backplane window program ... low=... mid=... high=...` plus bounded `sdio xfer chunk fn=1 ...` breadcrumbs so window-byte programming and per-chunk CMD53 address translation are explicit in the serial log, and same-window accesses now emit `backplane window reuse ...` instead of replaying the SBADDRLOW/MID/HIGH CMD52 triplet. Small CMD53 byte-mode transfers now drive SDHCI as a one-block transfer with block-count enable asserted, so a stalled `data-wait` on these logs indicates a transport failure rather than a half-programmed host byte-mode transfer. AI core-control writes no longer do a 32-bit read-modify-write before programming `AI_IOCTRL` or `AI_RESETCTRL`; the general `AI_IOCTRL` path still uses a direct byte-sized CMD52 write on the already-windowed unflagged Function 1 address, SOCRAM `assert-reset` emits `firmware core-ctrl reset-write mode=cmd53-word-windowed fallback=cmd52-byte-current-window-rewindow ...`, SOCRAM `clear-reset` now emits `firmware core-ctrl reset-write mode=cmd53-word-windowed fallback=cmd52-byte-current-window ...`, and the surrounding reset path now splits the first release edge into `stage=clear-reset-primary` and `stage=clear-reset-retry`. If that primary reset-release CMD53 write fails, root-task now recovers with `sdhci recover stage=core-ctrl-reset-clear-cmd52-current-window mask=cmd+data cache=preserved restored_window=... shadow_window=... fn=...` and retries only the current-window CMD52 path. The outer `clear-reset-write-retry` stage now skips a second `cmd53-word-windowed` replay and instead emits `stage=clear-reset-retry ... path=cmd52-byte-current-window cache=preserved` before retrying the preserved current-window write directly; a successful preserved retry still emits `firmware core-ctrl reset-clear stage=cmd52-current-window-ok ...`. If that SOCRAM retry-2 current-window write still fails with `sdhci-command-error`, root-task now emits `stage=clear-reset-retry-assumed-committed ... reason=socram-release-edge-timeout`, restores the cached backplane window from the shadowed `AI_RESETCTRL` access, and continues into the existing deferred-readback/postreset probe so the next breadcrumbs prove whether reset release actually committed before the transport dropped. The first SOCRAM post-release `AI_IOCTRL` write now emits `firmware core-ctrl postreset-write mode=cmd52-byte-current-window no-rewindow ...`; if that current-window write still returns `sdhci-command-error`, root-task now emits `stage=postreset-clock-en-assumed-committed ... reason=socram-postreset-write-timeout`, restores the cached backplane window from the shadowed `AI_IOCTRL` access, and continues into the readback path so the following breadcrumbs prove whether the post-reset `FGC|CLK` write committed before transport dropped. The first SOCRAM `postreset-clock-en-readback` `AI_IOCTRL` probe now keeps that same cached window alive across recovery, emitting `sdhci recover stage=core-ctrl-postreset-cmd52-current-window mask=cmd+data cache=preserved ...`, and stops after the direct current-window CMD52 read instead of immediately re-entering `SBADDRLOW` rewindowing, so the next remaining timeout identifies the actual post-reset read edge instead of a rewindow replay. If that direct current-window read still returns `sdhci-command-error`, root-task now emits `stage=postreset-clock-en-read-deferred io=... reason=socram-fragile-postreset-read`, restores the cached backplane window from the shadowed `AI_IOCTRL` access again, and continues into the remaining readback/verify path so the next breadcrumbs prove whether the only remaining blocker is the subsequent `AI_RESETCTRL` read rather than the first post-reset `AI_IOCTRL` probe itself. That following SOCRAM `AI_RESETCTRL` confirmation read now also stays on the preserved-window/no-rewindow path; if it still returns `sdhci-command-error`, root-task emits `stage=postreset-reset-read-deferred reset=... reason=socram-fragile-postreset-reset-read`, restores the cached backplane window from the shadowed `AI_RESETCTRL` access again, and completes `verify` with the already-cleared reset value so the next remaining failure is beyond the post-reset reset-confirmation probe. After that SOCRAM `verify` stage, the first ChipCommon config write now also fences the exact handoff from the SOCRAM `AI_RESETCTRL` window into `firmware stage=chipcommon-config`: if the redundant `SBADDRLOW` replay times out while only the middle window byte actually changes, root-task emits `stage=chipcommon-config-retry ... reason=mid-byte-only-window-switch`, recovers SDHCI command/data state, rewrites only `SBADDRMID`, restores the cached target window with `stage=chipcommon-config-window-retarget`, and retries the first ChipCommon config write directly so the next remaining failure is beyond the initial post-reset ChipCommon window transition. The outer reset sequence still splits that edge into `stage=postreset-clock-en-write`, `stage=postreset-clock-en-write-ok`, and `stage=postreset-clock-en-readback` so logs distinguish the first post-reset write from the first post-reset readback while keeping the SOCRAM `AI_IOCTRL` edge on the direct byte path. Once a core is already held in reset the `in-reset-configure` `AI_IOCTRL` path now first checks whether the asserted-reset write would be redundant for SOCRAM, emitting `in-reset-configure-skip ... reason=redundant-after-assert` and reusing the pre-reset `FGC|CLK` state when the requested hold value matches the value already written before reset. When that SOCRAM skip path is taken immediately after a fresh reset assert, the next `AI_RESETCTRL` readback is also deferred and root-task emits `in-reset-ready-read-deferred ... reason=redundant-after-assert` instead of forcing another post-assert probe while Function 1 is still fragile. The SOCRAM `core_reset` path now also skips its redundant re-entry through `core_disable`, emitting `core-reset ... stage=skip-disable reason=held-reset-from-prior-disable`; if that path already established the same held-reset `FGC|CLK` value, root-task now emits `core-reset ... stage=pre-clear-in-reset-configure-skip ... reason=redundant-held-reset-from-prior-disable` and avoids replaying the same SOCRAM `AI_IOCTRL` write before `clear-reset`, so the next remaining failure can move to reset release itself instead of a redundant held-reset replay. ARMCR4 non-redundant in-reset `AI_IOCTRL` writes still emit `firmware core-ctrl in-reset-write mode=cmd53-word-windowed-in-reset ...` and first use the flagged word-sized backplane write path before falling back to CMD52 recovery, while SOCRAM held-reset `AI_IOCTRL` replays now emit `firmware core-ctrl in-reset-write mode=cmd52-byte-current-window fallback=cmd52-byte-rewindow ...` so the fragile SOCRAM prepare stage stays on the already-windowed byte path. The general access breadcrumbs still print both `bus=...` and `trace_bus=...` so the serial log shows the exact unflagged byte address used for live CMD52 writes and the flagged address used for window tracing. The AI core-control sequence otherwise follows the upstream Broadcom order: write `FGC|CLK` before asserting reset, re-apply `FGC|CLK` while the core is held in reset, then clear reset and leave `CLK` enabled; on Pi 4 SOCRAM, that explicit re-apply step is now skipped only when the immediately preceding `skip-disable` path already left the identical held-reset value in place. ARMCR4 uses the upstream `CPUHALT` bit during disable/release so the firmware CPU stays parked until the final release. If the primary word read path times out, root-task now first retries recovery against the current backplane window with CMD52 and only then falls back to a rewindowed CMD52 pass; reset-register writes now log stage-specific reset breadcrumbs before trying current-window CMD52 recovery, and the SOCRAM `clear-reset` path preserves the cached backplane window across that recovery instead of taking an immediate rewindow detour, while in-reset `AI_IOCTRL` attempts still log `write8-in-reset` fallback breadcrumbs before dropping back to CMD52 rewindow recovery. The serial log also emits `sdhci recover stage=cmd-wait|cmd-error|data-wait|data-error|finish-wait|finish-error mask=cmd+data` when host recovery is forced. The same recovery path now emits paired `sdio shadow ...` breadcrumbs that preserve the last programmed backplane window, Function 1 bus address, and cached chipclk/wake/sleep/cardcap control values without issuing extra probe traffic after a failure. On the pre-upload path, when HT clock never reports ready after the shorter soft wait, firmware upload now stays on the startup/restore SDIO clock instead of climbing into speculative higher-clock retries that only added minutes of boot time on affected boards. That soft-timeout path also now preserves the writable `CHIPCLKCSR` request bits and layers `FORCE_HT` on top for the later firmware-channel / Function 2 transition, so the transport shadow no longer collapses to a bare `0x02` before post-load bring-up. If the post-upload HT status bit still sticks low while ALP stays available, the log now emits `firmware stage=wait-ht-clock timeout-soft ... action=continue-with-force-ht-refresh` and lets the firmware-channel handshake prove readiness before the final failure verdict. The first firmware-channel mailbox `CMD52` edge is now treated as part of that same bounded transport-recovery path, so a transient `sdio-cmd52-write` or `sdio-cmd52-read` during `setup-firmware-channel` or `wait-firmware-ready` emits the existing `action=recover-retry` breadcrumbs instead of terminating Wi-Fi bring-up immediately on the first post-HT mailbox access.
- Wi-Fi SDIO transfer failures now emit a decoded CMD53 metadata line (`op/fn/addr/inc/blk/count/blksz/blkcnt/flagged/trn`) immediately before the failing `sdhci xfer error ...` breadcrumb, and the AI core-control path emits `firmware core-ctrl access ...` plus `firmware core-ctrl fallback ...` breadcrumbs that name the exact backplane window, flagged bus address, byte shift, value, and fallback direction used for each latched write/readback. Those lines are the primary evidence for distinguishing a CMD52 control-path failure from a CMD53 fallback failure.
5. Validate diagnostics surfaces:
- on the manifest-default boot, `netstats` includes `mode=static policy=wired active=wired standby=none addr_src=manifest-static ip=192.168.10.42 gateway=192.168.10.1 dhcp=disabled`
- on the manifest-default boot, `netstatus` prints `ip=192.168.10.42 gateway=192.168.10.1 src=manifest-static dhcp=disabled`
- on DHCP boots, `netstatus` prints the compact lease state (`ip=<lease-ip> gateway=<gw> src=dhcp-lease dhcp=bound`) so wrapped serial consoles still show the active address.
- `nettest` targets the active wired address only.
6. Current limitation:
- explicit `policy=wifi` now supports both `dhcp` and `static` when credentials are present.
- `auto` remains DHCP-only and still tries Wi-Fi first when credentials are present, but Milestone 26b is not complete until on-device Pi 4 serial logs prove join + DHCP and `auto` fallback behavior.

## macOS U-Boot debug harness (fast iteration)
- Purpose: validate U-Boot scripts/env/network setup behavior quickly before hardware retest.
- Build QEMU U-Boot:
`make -C third_party/u-boot qemu_arm64_defconfig`
`make -C third_party/u-boot CROSS_COMPILE=aarch64-linux-gnu- -j$(sysctl -n hw.ncpu)`
- Run:
`qemu-system-aarch64 -machine virt -cpu cortex-a57 -m 2048 -nographic -bios third_party/u-boot/u-boot.bin`
- In harness, test env/network primitives (`printenv`, `setenv`, `dhcp`, `ping`, `tftpboot`) and script logic.
- Limitation: this harness does not prove Pi 4 USB keyboard, HDMI output, or GENET fidelity; those are hardware-only checks.

## Milestone 26 boot evidence requirements
- `/proc/boot` must include:
- `manifest.sha256=...`
- Milestone 26 baseline: `manifest.hw.no_nic=true` and `manifest.hw.networking=disabled-m26-baseline`
- Milestone 26a networking: `manifest.hw.network.enabled=true`, `manifest.hw.network.backend=bcmgenet-v5`, `manifest.hw.networking=enabled-static-ipv4`
- `attestation.bound_manifest_sha256=...`
- `attestation.evidence_sha256=...` (when attestation enabled)
- Keep before/after proof for the same profile family:
- baseline no-NIC transcript (Milestone 26),
- NIC-enabled transcript (Milestone 26a).
- Pi 4 local-seat proof must show:
- HDMI displays `cohesix>` prompt.
- USB keyboard input reaches the existing root-console parser.
- Typed commands produce visible responses on HDMI with deterministic ordering relative to serial.
- A stale firmware DT handoff with `xhci` marked `status = "disabled"` does not strand local-seat: root-task ignores that stale node as an active runtime source, rejects leaked DMA-alias values as non-MMIO, keeps VL805 ECAM preseed on a map-only path because live ECAM reads still correlate with fatal Pi 4 halts, and emits concise candidate-decision breadcrumbs before rejecting an xHCI MMIO source. On the standard Pi 4 U-Boot path, runtime treats the bootloader-exported high BAR as the only preferred local-seat runtime source and trusts it only when U-Boot exports the safe high-BAR contract and matching CAP snapshot after `usb stop`. If that contract is rejected, runtime emits `xhci runtime blocked action=reject-untrusted-high-bar ...` and leaves local-seat degraded instead of touching xHCI MMIO anyway.
- When the trusted high BAR is selected, local-seat replays the bounded runtime VL805 mailbox reset-notify through the shared long-lived request page and keeps xHCI ownership polling-driven with only the bounded bcm2711 PCIe child-INTx and bridge sinks. Those bounded sinks are now armed before controller construction and retained through the later controller-ready window, so the trusted `usb probe-kbd` golden path should emit `xhci irq policy ... stage=pre-controller-ready mode=poll-only+pcie-intx+bridge-sink ...` rather than the older `poll-only` line before `usb-oxide` touches live ownership state. If seL4 reports `RevokeFirst` while arming either bounded sink, runtime now fails that sink closed instead of reusing an arbitrary existing IRQ-handler cap; the path falls back to poll-only and leaves the existing owner untouched. Runtime now follows an explicit ordered xHCI attempt ladder and emits it in the live probe breadcrumb as `attempt=<n>/<total> origin=<...> handoff=<...> seed=<...>`, plus a companion `xHCI probe params ... seed_flags=0x.. snapshot=<bool> stop_seed=<bool> ring_seed=<bool>` line before `usb-oxide` touches live operational state. When a trusted stop-state snapshot is present on the bootloader-owned high-BAR path, runtime now leads with the seeded preserve-state handoff (`origin=stop-state-preserve`) instead of forcing an initial cold reset. If that preserve-first golden path still wedges, runtime falls back to `origin=uboot-fresh-init` and then `origin=seeded-cold-start`. Confirmed runtime reset outcomes still reuse the explicit reset-owned ladders, while only genuinely unconfirmed reset states emit `mailbox-reset-unconfirmed action=skip-candidate` and return to the UART console.
- The trusted CAP snapshot remains the runtime hint, and bootloader runtime ring pointers are no longer part of the Pi 4 handoff contract. On the active Pi 4 path, local-seat still rebuilds DCBAA, scratchpads, command ring, and event ring locally, but it now distinguishes whether the runtime attempt reused only the bounded halted stop-state seed (`seed=stop-state`) or ignored that seed entirely (`seed=none`) to mirror the U-Boot fresh-init path. Any remaining `xhci.diag stage=0x0253 tag=crcr-read-begin` breadcrumb is therefore part of runtime's own live seed path, not a preserved firmware-owned shortcut.
- The earlier preserved-controller deferred-ownership breadcrumbs remain available inside `usb-oxide` for generic regression work, but the active Pi 4 local-seat runtime no longer depends on them. The runtime either gets the trusted CAP snapshot plus the bounded stop-state seed and a mailbox-reset outcome strong enough for one of the two runtime-owned modes, or it fails gracefully back to serial-only input with explicit `[local-seat] ... unavailable` breadcrumbs.
- USB handoff diagnostics now include a root-task `xhci-handoff-contract state=... ready=... irq=... action=...` breadcrumb plus a companion `xhci-handoff-dtb source=... prestop=.../... poststop=.../...` line, and the staged U-Boot script emits a matching post-`usb stop` contract verdict (`safe|partial|absent`) after logging token state before reprobe, after forced reprobe, and in the pre-stop snapshot. The staged U-Boot serial path now also emits a single `xhci-reprobe-result reset=<success|failed> ready=<0|1|absent> irq=<0|1|absent> halted=<0|1|absent> safe=<0|1|absent> input=<0|1>` breadcrumb immediately after the forced `usb reset`, and the actual `xhci-pci` driver now emits `[cohesix:xhci-pci] stage=probe-entry|init|probe-ready|remove-entry|remove-ready ...` breadcrumbs with BDF, exported BARs, PCI command, and the live `coh_xhci_*` handoff state including `usbcmd`, `usbsts`, `iman0`, `halted`, and `safe`; on stop failures it emits `stage=remove-deregister ... ret=<err>` or `stage=remove-handoff-unsafe ... ret=<err>` instead of rebooting the board. The U-Boot `usb` command now propagates `usb stop` / `usb reset` failures back to the script, and the staged boot script explicitly clears the live `coh_xhci_*` handoff tokens before `usb stop` so stale probe-time `ready/irq` bits cannot masquerade as a post-stop contract. That makes the reprobe outcome visible in serial logs even before `usb stop` or `bootm`, and it shows whether the controller driver itself ever exported a safe handoff contract. The staged boot script no longer assumes `CONFIG_PREBOOT` already exercised the same xHCI path that local-seat needs for handoff; it uses `pci enum; usb start` only for menu/input bootstrap, then forces a real `usb reset` immediately before handoff capture so the active controller path is reprobed and any exported `coh_xhci_*` contract reflects that fresh session. Final DTB handoff sources now record whether the contract was `*-post-stop-safe`, `*-partial`, `*-absent`, or `reprobe-usb-stop-failed`, so a missing or rejected cold-start handoff stays diagnosable even when the controller is never trusted at runtime. The minimal DTB contract now contains BARs, PCI command, CAP snapshot, stop-state registers, and trust tokens only; it no longer mirrors bootloader-owned ring pointers into `/chosen/cohesix,*`.
- If firmware DT hands off an `xhci` node with `status = "disabled"`, local-seat must treat that `reg` as stale for runtime safety, ignore it as an active xHCI source, and either use a verified runtime source or emit explicit degraded/unavailable diagnostics instead of cycling between blind legacy aliases.
- Pi 4 firmware DT `xhci` `reg` values authored in the BCM2711 `0x7e...` SoC bus window must be translated through the DT `ranges` mapping into CPU physical addresses before local-seat runtime candidate selection. Boot breadcrumbs must make that translation explicit.
- Pi 4 U-Boot USB proof for common keyboards must show that downstream hubs do not get stuck in repeated EP0 halts during early hub-class port control: per-port `PORT_POWER` is deferred for unsafe downstream hubs, Apple `05ac:1006` still gets its 250 ms post-config settle, and delayed child `status-error` retries do not issue extra `PORT_POWER` writes.
- Pi 4 USB local-seat DMA buffers must remain below `0xC0000000` (first 3 GiB), matching the BCM2711 PCIe `dma-ranges` limit used by VL805/xHCI; within that window the device-visible PCIe DMA address is the same low address, not a synthetic high `0x4_...` alias.
- On the trusted Pi 4 xHCI handoff path, root-task routes the initial DCBAA, scratchpad, command-ring, and ERST/event-ring buffers through the audited DMA share/cache-maintenance seam before the first runtime ownership writes, while keeping `DCBAA[0]` clear until those first runtime ownership writes have been seeded and only then restoring the scratchpad-array pointer before `USBCMD.RUN`. U-Boot no longer mirrors pre-handoff `DCBAAP` / `CRCR` / `ERSTBA0` / `ERDP0` / `ERSTSZ0` state into `/chosen/cohesix,*`; runtime proves bring-up through its own first ownership writes and `xhci.diag` breadcrumbs.
- The current Pi 4 xHCI recovery path now splits the stronger runtime-owned snapshot contract from the conservative stop-state contract cleanly. Once runtime trusts the bootloader-exported high BAR contract, it always reuses the trusted bootloader capability snapshot only for CAP layout, replays the bounded runtime VL805 mailbox reset-notify step when present, and skips the known-unsafe constructor-time and init-time scrub writes plus the legacy-ownership claim and the early live halt revalidation read. The active runtime ladder is now explicit: once a trusted stop-state snapshot is available, runtime leads with the fully unseeded `uboot-fresh-init` path, retries the seeded `cold-start-from-snapshot` path second, and only then falls back to the seeded preserve/resetless diagnostic branch that matches the mailbox-reset outcome. That keeps the first attempt on the reset-owned path that most closely mirrors U-Boot's fresh controller init while preserving the seeded stop-state diagnostics as the immediate retry instead of the first shot. Genuinely unconfirmed mailbox states still fail back to serial-only input.
- As-built refinement: the conservative preserve-state branch no longer uses the old deferred `CRCR` ownership shortcut. That branch still seeds `DCBAAP`/`CRCR`/`ERST*`/`ERDP` from zero or the bounded stop-state snapshot only, takes the same bounded post-mailbox-reset blind settle before runtime MMIO ownership begins, skips the first live `USBCMD.HCRST` / `CONFIG` touch under `mode=preserve-controller-state`, and now completes the staged preserve-state event-ring table plus `DCBAAP` and `CRCR` before `USBCMD.RUN` while deferring the first live `DNCTRL` quiesce store until after `RUN`. That keeps the remaining startup fault isolated to the post-`RUN` ownership edge instead of letting a pre-`RUN` `DNCTRL` write hide the next controller-visible failure. On the active Pi 4 stop-state preserve branch, the first live `USBCMD.RUN` edge now uses the same U-Boot-style direct `writel()` path as the known-good upstream `xhci_start()` sequence rather than the earlier release-only helper, so the `xhci.diag` transition from `0x02e9 usbcmd-run-pre-store` to the matching post-store breadcrumb still cleanly distinguishes "the fence/drain wedged VL805" from "the RUN store itself wedged VL805". The abandoned stop-state `CRCR` handoff read is gone; any remaining `xhci.diag stage=0x0253 tag=crcr-read-begin` breadcrumb is now part of the runtime-owned path rather than the earlier stop-state shortcut.
- Pi 4 Wi‑Fi AI core-control diagnostics still surface both sides of the staged recovery path: flagged CMD53 windowed reads, byte-sized CMD52 current-window writes, preserved-window SOCRAM clear-reset retries, and the paired `sdhci recover ...` / `sdio shadow ...` breadcrumbs that make transport failures attributable. The active post-upload transport contract is now two-stage and bounded: root-task still makes the strict HT-clock attempt first and still requires real Function 2 readiness before using the control plane, but if the final stronger HT retry ends with `HT_AVAIL_REQ` latched, `ALP_AVAIL` high, `FORCE_HT` still asserted, and complete HT-assist shadow state, it now emits `action=continue mode=bounded-no-ht`, enters the slower no-HT transport profile, and lets `setup-firmware-channel` / `wait-firmware-ready` prove whether the mailbox path is actually alive instead of failing immediately at the PMU boundary. ARMCR4 post-reset proving is now also less self-defeating: once the Linux-style post-reset clock-enable write has landed and the transport has already been raised to the full control-plane clock, the first fragile ARMCR4 wrap-register readback is treated as advisory and emits the existing `readback-deferred ... reason=armcr4-fragile-postreset-read-assumed-core-up` verdict immediately, so the next remaining failure is the HT / mailbox side rather than another repeated CMD53 `phase=data-wait` reread of the same wrap registers. On that no-HT path, Linux-aligned transfer planning now keeps Function 1 firmware writes and Function 2 startup-link writes in CMD53 byte mode up to the 512-byte SDIO ceiling; 512-byte Function 2 frames therefore travel with a zero `cmd53_count` on the wire, while reply reads remain on the smaller conservative bursts. The first bounded no-HT control-plane bootstrap now also stays on the startup-safe SDIO link until firmware answers the first Function 2 exchange, so the initial ioctl no longer starts at data clock and then falls back after the first write/reply edge has already failed. Post-firmware-ready probes still emit `action=preserve-enable-latch ... reason=linux-aligned-no-clear` when F2 is already latched, strict reply recovery now does a real F2 disable/re-enable cycle when the card is still stuck in `ioex=0x06` / `iorx=0x02`, and the post-F2 SDIO setup now keeps the Linux-aligned CYW43455 `watermark`, `F2WM_ENAB`, and `MESBUSYCTRL` writes while also programming the Linux-style Function 2 interrupt masks (`function-int-mask` + `IENX`) even on the fragile no-HT path. The first passive startup-link ioctl reply wait still keeps the Linux-sized long grace window, but once the same control-plane edge has already entered startup-link rescue the wait ratchets down sharply on each no-progress cycle instead of reusing that full budget forever. Explicit `policy=wifi` boots also stop waiting for the association result inside `Cyw43NetDevice::new()`: once the control plane is up and the join request has been armed, root-task returns to the event pump, emits `[net-console] pending-link ... detail=wifi-associating ...`, and then completes association in bounded poll slices instead of blocking the whole boot on the AP/auth exchange. Only after that first successful reply does the existing promotion path raise the link back to the faster control-plane clock, which keeps the remaining failure surface focused on one startup-safe exchange instead of a promoted-first write plus fallback recovery. Failure snapshots now end with a compact `control-plane snapshot ... diag f2=... hints=... hm=... fm=... rf=...` line so the remaining reply-side fault is classifiable from one serial capture.
- The initial `setup-firmware-channel` block and the later control-plane rearm block are still transport-dependent, but the bounded no-HT path now keeps the channel on whatever clock the transport has already earned instead of bouncing those writes through an extra 400 kHz demote/restore cycle. On the strict path HAL still uses the bounded startup-clock write window and then restores the faster link after the mailbox / interrupt / device-control writes complete. On the bounded no-HT path HAL keeps the Linux-minimal CYW43455 mailbox-version / `HOSTINTMASK` / `watermark=0x60` programming on the current link, and the driver now logs `[cyw43] control-plane reply wait rearm policy=promoted-link|startup-link ...` so a single serial capture shows whether the remaining failure happened on a real promoted no-HT reply edge or only after a deliberate fallback to startup-link retry.
- On the bootloader-owned stop-state-only xHCI snapshot branch, runtime still uses the trusted CAP layout plus stop-state diagnostics, suppresses the early halt revalidation touch, stays on the seeded `mode=cold-start-from-snapshot` ladder, and still skips the first runtime `USBCMD.HCRST` / `CONFIG` edge (`xhci.diag stage=0x0234` / `0x0245`). The latest Pi 4 traces, however, moved the remaining failure from `CONFIG` to the first live pre-`RUN` `DCBAAP` low-word store, so the weak stop-state mailbox handoff is back on the deferred ownership ladder for `DCBAAP`, `CRCR`, `DNCTRL`, and now the event-ring publish as well. That keeps the next controller-visible edge focused on `USBCMD.RUN` or the later post-`RUN` ownership handoff instead of repeating pre-`RUN` ownership-store wedges. The old stop-state deferred `CRCR` read remains gone; any remaining `xhci.diag stage=0x0253 tag=crcr-read-begin` breadcrumb is now part of the runtime-owned path instead.
- When Pi 4 USB keyboard bring-up terminates in `detail=xhci-init`, local-seat also emits `xhci diag summary context=keyboard-init stage=... tag=... a=... b=... c=...` so the last xHCI state survives diag rate limiting and can be matched directly against the earlier `xhci.diag` ladder.
- As-built safety rule: once the UART root shell is live, the event pump no longer auto-probes the Pi 4 USB keyboard path on its own. Local-seat keeps HDMI mirroring active, but backend keyboard polling stays deferred with `runtime keyboard poll deferred action=serial-shell-first` until an explicit runtime action enables it, so a toxic xHCI probe cannot wedge the board after boot has already succeeded.
- The explicit runtime action is the serial/local-seat `usb` debug surface: `usb status` reports whether the local-seat backend is attached and whether runtime keyboard polling is still deferred, `usb enable-kbd` arms deferred runtime probing, and `usb probe-kbd` immediately runs one on-demand keyboard probe pass after arming polling. `usb probe-kbd` now emits a `golden_path preflight ...` route/edge pair before xHCI MMIO starts, so even a halt before return still leaves the chosen origin/handoff/seed/halt-guard plus the first expected stop-revalidation edge in the serial transcript. The same serial-local debug surface continues to expose `wifi dump-state`, `wifi probe-ht`, `wifi load-fw`, and `wifi retry` for staged CYW43455 bring-up.
- After firmware/NVRAM staging, ARMCR4 bring-up now retries post-reset single-byte reads through the same Function 1 CMD53 backplane ladder as normal core access instead of forcing those edges down a CMD52-only path first: the serial log should now show `firmware core-ctrl access op=read8 mode=cmd53-windowed-read32-cmd53-byte-current-window fallback=cmd53-byte-rewindow ...` for `postreset-clock-en-readback` and `armcr4-core-up` readbacks, and SDHCI transfer-command / transfer-finish failures on those edges are treated as the same bounded fragile-postreset-read class as the older command-timeout cases. The expected follow-up is therefore either a real `ready io=... reset=...`, a bounded `readback-retry ... reason=armcr4-fragile-postreset-read`, or the existing deferred `postreset-clock-en-read-deferred ...` / `readback-deferred ... reason=armcr4-fragile-postreset-read-assumed-core-up` verdict when the transport is still settling.
- The `wait-ht-clock` serial evidence is now decoded as well as timed: alongside `request=...`, `alp-request=...`, and timeout breadcrumbs, root-task now emits `status=request-readback|ht-rerequest-readback|timeout-soft|timeout-hard ... ht_req=<y|n> alp_req=<y|n> force_ht=<y|n> clkreq_off=<y|n> alp=<y|n> ht=<y|n> wake=0x../<y|n> sleep=0x../<y|n> cardcap=0x../<y|n>`, so the next failure shows whether the PMU request bits latched, whether ALP stayed up, and whether `HT_AVAIL` is the only bit that never advanced.
- The first SOCRAM reset-release path now adds `pre-clear-in-reset-configure ... reason=required-before-clear-reset`, but when `skip-disable` already established the same held-reset value it emits `pre-clear-in-reset-configure-skip ... reason=redundant-held-reset-from-prior-disable` and avoids replaying that identical write before `clear-reset-prewrite-delay ... reason=socram-fragile-first-write`. On failure it still makes a single `clear-reset-write-retry ... reason=socram-fragile-first-write` recovery attempt before the existing deferred `clear-reset-read-deferred ...` readback path, and that first clear-reset recovery now preserves the cached backplane window while skipping an immediate `cmd52-byte-rewindow` detour. The second attempt also now skips replaying the already-failed `cmd53-word-windowed` reset-clear write and instead retries the preserved `cmd52-byte-current-window` path directly, so logs distinguish a redundant held-reset replay from a transient reset-release edge stall instead of a self-inflicted reset-clear word-write replay.
- Pi 4 Wi‑Fi firmware load now emits `firmware stage=pre-reset-ht-assist` before the ARMCR4/SOCRAM reset-heavy path whenever the cached chip-clock state does not already request or force HT. That pre-reset assist keeps `wake`, `cardcap`, `chipclk`, and `sleep` primed before `stage=armcr4-disable` and `stage=socram-disable`, so a remaining fault after the earlier backplane-window fix identifies a true reset-edge transport problem rather than a missing HT assist request. The later `wait-ht-clock` ALP-prime substep now also preserves the forced HT / retry request bits instead of dropping back to a bare ALP-only request, emits `alp-request=0x..`, and gives the final stronger required retry a larger bounded wait budget (`action=retry-stronger-request ... wait_loops=...`) with chunked `progress=wait-ht-clock polls=... csr=...` checkpoints. If that final retry still has the bounded no-HT contract latched after one soft-wait progress chunk, root-task now emits `action=bounded-no-ht-early-exit ... shortcut=...` and cuts over immediately instead of burning the rest of the larger HT poll budget. On that bounded no-HT path, `wait-firmware-ready` now holds the mailbox poll on the startup SDIO clock that already succeeded for `setup-firmware-channel`, retries the mailbox read through both the incrementing Function 1 path and an explicit byte-read Function 1 fallback instead of stopping at the SDIO-core windowed read, and only after one bounded recovery pass plus those lower-touch fallback reads may treat repeated mailbox-read failures as provisional ready so control-plane init can prove or disprove the transport on live traffic. The Linux-inspired reply-side channel programming remains `mailbox-version -> Function 2 -> hostintmask -> watermark -> DEVICE_CTL(F2WM_ENAB) -> MESBUSYCTRL -> interrupts`, and Function 2 readiness still emits `sdio function-ready fn=2 action=prime-block-size ...` plus `sdio function2 ready-snapshot ... diagnosis=... corecontrol=... corestatus=... cc_f2rdy=...` so the next failure log distinguishes a true `IOR2` latch failure from the `f2-ready-hidden-from-cccr` case where CCCR `IORx` stays low even though SDIO core `corecontrol` already shows `CC_F2RDY`. The remaining promoted/no-HT failure mode is now classified more tightly: if the reply-side Function 1 backplane reads themselves are dying at SDHCI command phase, failure snapshots now preserve that SDHCI state and report the exact blocker as `cyw43-control-plane-sideband-command-stall` or `cyw43-control-plane-sideband-unreadable` instead of collapsing the whole path into the older generic `cyw43-function2-enable-latched-not-ready` bucket. HAL still keeps the startup-link fallback as a guardrail, and if the speculative higher-clock reply probe still stalls after that first proof write, the driver now drops back to the startup link, preserves the live card selection there (`action=sdio-host-live-resume-*`), and forces the host back to the startup-safe profile (`400000 Hz`, `4bit`) instead of renegotiating the live control path all the way down to `1bit`. Only if that live-card startup-link resume fails does HAL fall back to a fresh `CMD5` reattach (`action=sdio-host-reattach-*`), after which it waits once for the original outstanding ioctl reply on the slower link and, if that recovered reply path still times out, the first resend also stays on the startup link rather than re-promoting before control-plane bootstrap has proved a successful ioctl.
- When strict reply recovery reaches the Linux-aligned `ioex=0x06` / `iorx=0x02` state but the reply-side Function 1 sideband registers remain unreadable, HAL no longer treats that snapshot as an immediate terminal verdict. Instead it now emits `action=strict-recover-sideband-unreadable ... continue=pure-f2-startup-link`, marks the hint path unstable, rearms the startup-link reply path, and allows blind Function 2 speculative reply reads on that fallback even when the frame-length hint path only returns `0`. Those blind reply reads now keep the Linux `BRCMF_FIRSTREAD` shape as well: HAL pulls a 64-byte fixed-address Function 2 prefix first, validates the SDPCM header from that prefix, and only then requests the remainder of the reply instead of treating the whole speculative-frame capacity as one open-ended read. That fallback is now more aggressively bounded: passive startup-link reply wait stops at 16 empty polls, failure snapshots report `sideband_cycles=N/2`, startup-link rescue snapshots report `rescue_cycles=N/2`, and once `action=pure-f2-startup-link-exhausted` appears the final exact error is `cyw43-control-plane-pure-f2-startup-link-no-reply`, which means sideband hints are proven toxic and the remaining blocker is the Function 2 reply path itself rather than mailbox-version / hostintmask / watermark sideband programming. The bounded no-HT path now also locks the control plane back to the startup-safe `400000Hz` / `4bit` SDIO profile as soon as HT never stabilizes or promoted channel I/O proves unstable, and `wifi dump-state` reports that as `safe_profile_locked=<yes|no> safe_reason=<...>`. The post-firmware-ready strict Function 2 repoll no longer short-circuits before the explicit disable/re-enable attempt: once firmware-ready mailbox/version is valid and the board is already in the Linux-style armed state `latched-linux-configured-no-iorx`, HAL still emits `action=strict-repoll ...`, forces the real Function 2 repoll first, and now soft-continues directly into the bounded startup-link reply probe instead of failing fast when that exact latched snapshot remains sideband-unreadable. That strict-repoll budget no longer counts as a preserved-latch path: if `IOR2` is still low, HAL replays the real Function 2 enable/ready recovery instead of treating the stale `IOEX` latch as sufficient, while only the later bounded reply-probe and bypass budgets may preserve the Linux-shaped Function 2 enable state without another disable/re-enable cycle. The later `reply-strict-recovery` channel-rearm path now enforces that same rule explicitly: it emits `action=force-function2-ready-repoll ... reason=reply-strict-recovery-disallows-preserve-latch`, performs a real Function 2 clear/re-enable cycle, and will not take `slow-link-channel-rearm-preserve-f2` on that budget. Later no-HT reply recovery still preserves the proven-good Function 2 enable latch only on the bounded probe/bypass budgets; there `slow-link-channel-rearm` emits `action=preserve-function2-enable-latch` plus `action=skip-function2-ready-repoll`, re-primes the 512-byte Function 2 block size, and reapplies mailbox / interrupt / watermark programming around the existing latch instead of turning a blind reply-read miss into another `sdio-function2-ready-timeout`.
- Pi 4 xHCI bring-up now emits explicit pre-`RUN` decision breadcrumbs so the remaining USB stall can be classified without guessing: `xhci probe params ... axi_setup=<yes|no>` / `xhci probe begin ... axi_setup=<yes|no>` now confirm from the serial wrapper whether the Broadcom AXI vendor-register quirk will even run before control enters the xHCI constructor. Trusted firmware-handoff starts (`cold-start-from-snapshot`, `resetless-reinit`, `preserve-controller-state`) now leave `axi_setup=no` and reuse the bootloader-programmed AXI attribute state directly, while only the genuinely live runtime-init path keeps `axi_setup=yes`. `0x0111` records entry to the very first Broadcom AXIWRA vendor-register read, `0x0112` records that write-attribute register offset plus its before/target values, `0x0113` records the matching write-attribute readback/expected pair after the store, `0x0116` records entry to the first AXIRDA vendor-register read, and `0x0114` / `0x0115` do the same for the AXI read-attribute register. That means a serial stop at `0x0105` still identifies generic MMIO remap, a stop at `0x0111` now proves runtime took the wrong live AXI-setup branch and wedged on the first AXIWRA vendor load itself, and a stop at `0x0116` isolates the first AXIRDA vendor load after AXIWRA completed. `0x0217` records the halt-revalidation decision (`handoff`, seed-flag bitmask, and whether the live rereads will be skipped), `0x0218` records entry into the skip branch, `0x0213` / `0x0214` bracket the first live `USBSTS` halt-revalidation read, `0x0215` / `0x0216` bracket the first live `USBCMD` halt-revalidation read, `0x02eb` now marks entry into the final U-Boot-style pre-store barrier, `0x02ea` marks that barrier completing, `0x02e9` means runtime reached the exact volatile-store edge with the final MMIO pointer/value pair in hand, `0x02da` means the `USBCMD.RUN` store returned, `0x02db` records the first `USBCMD` readback after that store, `0x02dc` records the first `USBSTS` sample after `RUN`, `0x02dd` gives bounded HCH-wait progress snapshots, and `0x02de` records the final timed-out `USBCMD`/`USBSTS` pair before `0x0272`. The snapshot-backed stop-state cold-start path now emits the same skip decision before it bypasses those live halt rereads, so a missing `0x0218` after a seeded `origin=seeded-cold-start` runtime attempt identifies a seed-propagation bug rather than a post-`RUN` ownership edge. That seeded cold-start branch still trusts the stop-state MMIO snapshot enough to skip the first toxic live halt/reset rereads, but it now keeps the full weak-handoff ring picture behind `RUN`: `DCBAAP`, `CRCR`, `DNCTRL`, and the event-ring publish all stay on the deferred post-`RUN` ownership ladder. The expected `pre-run-publish-mask` for that branch therefore now clears the `DCBAAP|CRCR|DNCTRL|ERST/ERDP` bits entirely, so the next controller-visible edge is `USBCMD.RUN` or the later deferred ownership handoff rather than any remaining pre-`RUN` ring-store experiment. `ERSTSZ` / `ERSTBA` publish stages are still shared across both direct and deferred paths (`erstsz-publish-*`, `erstba-publish-*`), so a stop at `0x02c3` still means the `ERSTSZ` store wedged and a stop at `0x02c6` / `0x02c8` still means the low/high halves of the `ERSTBA` handoff wedged, but the `local-seat` exact-issue mapping now also splits `DCBAAP` low/high stalls into pre-`RUN` and post-`RUN` buckets so the next Pi 4 capture proves which side of `RUN` the weak handoff actually reached. The snapshot-backed `mode=preserve-controller-state` path no longer performs a fresh live `USBCMD` seed read on the trusted stop-state handoff; it now seeds `RUN` from the bootloader-exported stop-state snapshot instead, with `0x02ee` logging the masked snapshot seed and composed `RUN` value before the store, while fully unseeded paths still use the live `0x02ec` / `0x02ed` read-modify-write ladder and bare paths emit `0x02ef`. The preserve-state branch now goes one step further: if the trusted stop-state path already reports HCH clear, runtime emits `0x02e8 fw-handoff-trusted-usbcmd-run-skip` and preserves the bootloader-owned RUN state instead of replaying a redundant live `USBCMD.RUN` store on the same IRQ27 edge. That same preserve-state branch now also rejoins live interrupt-quiesce at the first safe post-ready boundary: it no longer skips the trusted `USBSTS` clear or the post-ready `IMOD` / `IMAN` zeroing, so the next controller-visible edge after `0x02e8` is the deferred ERST publish rather than stale firmware moderation or pending-interrupt state. The preserve-state branch has been pulled back to the same U-Boot-style `writel()` barrier discipline used by the known-good upstream `xhci_start()` path, so a serial stop at `0x0213` means the first live `USBSTS` halt read never returned, a stop at `0x0215` means the follow-up `USBCMD` read never returned, a stop at `0x0275` still identifies the remaining live-seed-read path rather than the trusted preserve-state branch, a stop at `0x0248` / `0x0249` means the direct `DCBAAP` low/high store wedged, a stop at `0x0254` / `0x0255` means the direct `CRCR` low/high store wedged, a stop at `0x0277` / `0x0278` means the direct `ERDP` low/high store wedged, a stop at `0x02c3` means the `ERSTSZ` store itself wedged, a stop at `0x02c6` / `0x02c8` means the low/high halves of the `ERSTBA` handoff wedged, a stop at `0x02eb` with no `0x02ea` means the final U-Boot-style MMIO barrier itself wedged, a stop at `0x02e9` with no `0x02da` means the volatile `USBCMD.RUN` store itself wedged, and only a path that reaches `0x02da`/`0x02db`/`0x02dc` should be treated as a post-`RUN` status-handling problem rather than a live `RUN` edge failure. When local-seat emits the bounded `xhci diag summary ...` line, it now also appends `exact=live-usbsts-read-before-run`, `exact=live-usbcmd-read-before-run`, `exact=pre-run-dcbaap-low-store-wedged`, `exact=pre-run-dcbaap-high-store-wedged`, `exact=post-run-dcbaap-low-store-wedged`, `exact=post-run-dcbaap-high-store-wedged`, `exact=pre-run-crcr-low-store-wedged`, `exact=pre-run-crcr-high-store-wedged`, `exact=pre-run-erdp-low-store-wedged`, `exact=pre-run-erdp-high-store-wedged`, `exact=pre-run-erstsz-store-wedged`, `exact=pre-run-erstba-low-store-wedged`, `exact=pre-run-erstba-high-store-wedged`, `exact=usbcmd-run-barrier-wedged`, or `exact=usbcmd-run-store-wedged` for the most important pre-`RUN` and immediate post-`RUN` failure classes.
- Boot must fail before ticket registration if:
- attestation is required/enabled and policy cannot be satisfied.
- `hw.local_seat.required=true` and local-seat initialization cannot be satisfied.
- If `hw.local_seat.required=false`, runtime must degrade to serial-only diagnostics with explicit `[local-seat] degraded ...` boot lines.

## Bootloader/HAL ownership boundary
- Bootloader-owned (pre-handoff): Pi firmware + U-Boot setup, media loading, pre-boot env/network commands.
- Cohesix-owned (post-seL4): HAL-backed runtime device access only (UART, local-seat paths, attestation plumbing, network handoff points).
- Root-task runtime code must not call bootloader/firmware services directly after seL4 entry.
