<!-- Copyright 2026 Lukas Bower -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Purpose: Document Cohesix Raspberry Pi 4 hardware bring-up workflows and constraints using the official U-Boot + binary image path. -->
<!-- Author: Lukas Bower -->
# Hardware Bring-up (As-Built)

## Scope
- Cohesix supports two bring-up paths:
- QEMU `aarch64/virt` (development/CI baseline).
- Raspberry Pi 4 (`bcm2711`) via upstream-style boot chain: `Pi firmware -> U-Boot -> seL4 image -> root-task`.
- Milestone 26 defines the strict no-NIC baseline on Pi 4; Milestone 26a adds profile-gated GENETv5 + static IPv4; the current 26b as-built state adds the interactive U-Boot policy wizard, DTB `/chosen/cohesix,*` network-policy handoff, Cohesix-owned xHCI/VL805 local-seat cold start by default, opt-in U-Boot halted stop-state diagnostics, wired DHCP, and the staged Pi 4 Wi-Fi runtime path.

## Canonical Pi 4 boot chain
1. Pi boot firmware loads `start4.elf` and `fixup4.dat`.
2. Firmware loads `u-boot.bin` (from FAT boot partition).
3. U-Boot loads the staged Cohesix payload (`cohesix-image-arm-bcm2711`) and the padded staged Pi 4 DTB (`bcm2711-rpi-4-b.dtb`) using `fatload`.
4. U-Boot applies any saved/runtime Cohesix policy into DTB `/chosen/cohesix,*`, quiesces the USB host stack, and transfers control with `bootm`.
5. seL4 enters root-task; Cohesix reaches `cohesix>` prompt.

## Manifest profiles
- Development profile: `configs/root_task.toml` (`profile.name = "virt-aarch64"`).
- Pi 4 baseline profile (Milestone 26 no-NIC): `configs/root_task_uefi_aarch64.toml` (`profile.name = "uefi-aarch64"` migration alias).
- Pi 4 networking profile (Milestone 26a/26b policy baseline): `configs/root_task_pi4_uboot_aarch64.toml` (`profile.name = "pi4-uboot-aarch64"`).
- `coh-rtc` enforces profile gates:
- Milestone 26 baseline: `hw.no_nic=true`, `features.net_console=false`.
- Milestone 26a/26b networking: `hw.network.enabled=true`, `hw.network.backend=bcmgenet-v5`, bounded `hw.network.mode` (`off|static|dhcp`), bounded `hw.network.interface` (`wired|wifi|auto`), DHCP retry/timeout bounds, required `net` device declaration, and bounded non-zero static IPv4 (`prefix_len=1..32`) when `mode=static`.
- declared `uart` + `rtc` devices
- local-seat declarations when `hw.local_seat.enabled=true`
- attestation policy/device requirements when `hw.attestation.enabled=true`

## Pi 4 build + boot commands
1. Build seL4 Pi 4 image:
`cmake --build seL4/build_UEFI --target images/sel4test-driver-image-arm-bcm2711`
2. Build U-Boot (Pi 4):
`make -C third_party/u-boot rpi_4_defconfig`
`make -C third_party/u-boot CROSS_COMPILE=aarch64-linux-gnu- -j$(sysctl -n hw.ncpu)`
3. For a full clean Pi 4 rebuild, use:
`scripts/pi4-image-build.sh --clean --manifest configs/root_task_pi4_uboot_aarch64.toml`
- `--clean` removes `root-task` Cargo artifacts, cleans and rebuilds the Pi 4 seL4 U-Boot build tree, and runs a fresh `rpi_4_defconfig` U-Boot rebuild before staging/flashing.
- On macOS ARM64, `--clean` also injects the Homebrew OpenSSL include/lib paths into the U-Boot host-tool build so `mkimage` and the other host utilities rebuild against the expected OpenSSL installation.
4. Prepare FAT boot partition with:
- Pi firmware files (`start4.elf`, `fixup4.dat`, required DTB/overlay files),
- `u-boot.bin`,
- `cohesix-image-arm-bcm2711` (copied from seL4 `images/sel4test-driver-image-arm-bcm2711`),
- Cohesix manifest artifacts (`manifest.json`, `manifest.sha256`, and related packaged assets).
5. Ensure `config.txt` includes:
- `arm_64bit=1`
- `enable_uart=1`
- `kernel=u-boot.bin`
- `dtoverlay=upstream-pi4`
- `core_freq=250` (keeps UART handoff deterministic during firmware -> U-Boot transition)
6. Boot from U-Boot shell:
- `fatls mmc 0:1`
- `fatload mmc 0:1 ${loadaddr} cohesix-image-arm-bcm2711`
- `go ${loadaddr}`

## U-Boot networking setup (for 26a/26b prep)
- Milestone 26a uses static IPv4 in runtime. The current 26b Pi 4 path adds an interactive U-Boot wizard plus persisted `cohesix.env` policy handoff through a staged padded DTB and U-Boot `bootm`, while keeping manifest defaults authoritative when the handoff is absent or invalid.
- Typical env setup:
- `setenv autoload no`
- `setenv ipaddr <board-ip>`
- `setenv serverip <host-ip>`
- `setenv ethact <interface>`
- The staged `boot.cmd` now presents a Linux-style numbered wizard over the existing U-Boot consoles:
- `Continue with existing config` is the default action when saved Cohesix policy already exists in `cohesix.env`.
- `Boot with manifest defaults` is the default action when no saved Cohesix overrides exist.
- `Configure networking` walks DHCP `ON|OFF`, `wired|wifi`, Wi-Fi credentials when needed, and static IPv4 prompts only when DHCP is off.
- `Save current settings and reboot` persists only the user-facing Cohesix policy fields to `cohesix.env` on the FAT boot partition; it does not rewrite the manifest or generic U-Boot environment.
- The wizard intentionally uses `askenv`-based numbered prompts instead of U-Boot `bootmenu`, because upstream U-Boot documents `bootmenu` as an ANSI-terminal path and the simpler prompt flow is more reliable on the Pi 4 HDMI + USB-keyboard bring-up path while still preserving serial control via `minicom`.
- The script reloads only `cohesix.env` from the FAT partition, keeps the PREBOOT console serial-first, bootstraps menu/input USB with `pci enum; usb start` only when the wizard actually needs an input session, and switches `stdin` to `usbkbd,serial` only after that session is live so the HDMI wizard prefers the USB keyboard while retaining serial fallback. The menu no longer emits the old bring-up-only USB diagnostic snapshots or the extra USB diagnostics submenu; the default runtime path stops U-Boot USB before handoff, clears xHCI trust tokens, and leaves root-task to cold-start the high-BAR controller from Cohesix-owned mappings.
- The staged `boot.cmd` defaults to `COHESIX_PI4_UBOOT_XHCI_HANDOFF=0`: before `bootm` it switches input back to serial, clears stale `coh_xhci_*` trust tokens, runs one `usb stop` when a U-Boot USB input session was active, and exports only the non-authoritative stopped register seed even when no U-Boot USB session was active. Root-task may use the Linux-proven Pi 4 VL805 BAR0 `0x600000000` capture as bounded layout evidence, but capture-only state does not authorize fresh `DCBAAP`/ring/RUN publication. Fresh high-BAR ownership now requires the default HAL-owned BCM2711 EXT_CFG proof path: bind the PCIe/INTx sinks, mask/clear PCIe host sources, power the USB HCD domain through the VideoCore mailbox (`POWER_DEVID_USB_HCD=3`), validate live link/RC status, run one bounded HAL-owned BCM2711 root-complex reset/window init if that status is missing, drain posted reset/window writes with same-block readbacks, wait through a conservative spin-backed U-Boot/PCIe CEM 100 ms post-PERST target plus bounded 100 ms link-poll target, then either re-prove live status or promote only on exact live EXT_CFG proof of VL805 `01:00.0` (`1106:3483`, class `0x0c0330`, BAR0 translation, and COMMAND readback), and disable MSI for the poll-only lane. If that proof fails, the unseeded Cohesix-owned lane still returns before xHCI ownership writes. Setting `COHESIX_PI4_UBOOT_XHCI_HANDOFF=1` when staging exports the halted U-Boot stop-state into `/chosen` (`cohesix,xhci-mmio`, PCI command, `USBCMD`, `USBSTS`, `IMAN0`, and final trust tokens) for bounded diagnostics and fallback comparison. Neither path emits USB trace breadcrumbs, forces a handoff reprobe, records source labels, mirrors capability/pre-stop/post-stop diagnostic snapshots, or derives a PCI config-space aperture from the xHCI BAR.
- The Pi 4 U-Boot build stays on the seL4-aligned `usbkbd` interrupt-polling baseline (`CONFIG_SYS_USB_EVENT_POLL=y`) so the HDMI wizard matches the previously working Pi 4 input path.
- The SD staging step also writes Linux comparison helpers `brcmfmac-dyndbg.cmdline` and `brcmfmac-dyndbg.sh` to the FAT partition. Use the `.cmdline` contents when booting a Linux known-good image to capture brcmfmac/MMC/SDHCI debug from module load, or run the shell helper after boot to enable the same dynamic-debug selectors through debugfs. These files are diagnostics only; the Cohesix U-Boot path does not append Linux bootargs to the seL4 handoff.
- `scripts/pi4-image-build.sh` deliberately does not run `scripts/pi4_trace_normalize.py`. The build script owns deterministic image staging/flashing and has no authoritative post-boot serial input; trace normalization is a post-capture evidence step. A future hardware-in-the-loop wrapper may call build, flash, serial capture, and normalization in sequence, but the staging script itself should remain artifact-only.
- Optional Cohesix net-policy overrides mirrored into DTB `/chosen` by the staged boot script:
- `setenv coh_net_mode <off|static|dhcp>`
- `setenv coh_net_interface <wired|wifi|auto>`
- `setenv coh_static_ip <ipv4>` (mirrored into DTB `/chosen/cohesix,static-ipv4`; only applied when the effective mode is `static`)
- `setenv coh_static_prefix_len <1..32>` (mirrored into DTB `/chosen/cohesix,static-prefix-len`; only applied when the effective mode is `static`)
- `setenv coh_static_gateway <ipv4>` (mirrored into DTB `/chosen/cohesix,static-gateway`; optional and only applied when the effective mode is `static`)
- `setenv coh_wifi_ssid <ssid>` (mirrored into DTB `/chosen/cohesix,wifi-ssid`; used by the CYW43455 path when `coh_net_interface=wifi` or when `auto` prefers Wi-Fi)
- `setenv coh_wifi_psk <psk>` (mirrored into DTB `/chosen/cohesix,wifi-psk`; used by the CYW43455 path for open/WPA2-PSK join)
- The staged boot script now hands the saved `coh_wifi_*` variables directly to `fdt set` without mutating or persisting escaped shadow copies, so repeated boots and policy-file writes do not grow backslashes or corrupt WPA2 credentials. If an older card reports `Wi-Fi credential handoff overflow`, clear the saved `coh_wifi_ssid` / `coh_wifi_psk` values once and re-enter them through the wizard.
- The staged `bcm2711-rpi-4-b.dtb` is padded to 128 KiB before flashing, so U-Boot can add `/chosen/cohesix,*` properties in place without `fdt resize`.
- `setenv coh_show_logo <0|1>` (controls whether the staged `boot.bmp` splash is displayed on HDMI before the menu)
- The staged Pi 4 U-Boot build enables 24bpp BMP drawing, so the centered `boot.bmp` splash uses the same HDMI framebuffer path as `bmp display`.
- Pi 4 U-Boot now keeps `CONFIG_PREBOOT` on the seL4-aligned serial/video console path only; the HDMI wizard owns the first `pci enum; usb start` so boot no longer pays an unconditional duplicate USB-host bring-up before the menu. The default Pi 4 path still avoids a global scripted USB startup delay, but it keeps a targeted Apple `05ac:1006` keyboard-hub quirk in the U-Boot hub driver: a 250 ms post-config settle and a 5 s hub debounce timeout before downstream child probing.
- On first entry to the root menu, the staged `boot.bmp` copy of the Cohesix logo is shown centered while the wizard prepares the menu/input USB session, and the interactive menu is then drawn on a cleared console. The default splash path no longer burns an extra fixed sleep before menu bring-up.
- `ping ${serverip}`
- `fatwrite mmc 0:1 ${coh_policy_addr} cohesix.env ${filesize}` (only when explicitly persisting Cohesix policy)
- The staged `boot.cmd` copies these env vars into `/chosen/cohesix,*` on the staged DTB before `bootm`. If that DTB cannot be loaded or updated, the script aborts before handoff instead of silently booting with stale policy. Saved Cohesix policy persists across reboots via `cohesix.env`, but the manifest remains the build-time default and is never rewritten on the SD card.

## Milestone 26a Pi 4 network checklist
1. Build/validate the QEMU U-Boot harness:
- `scripts/uboot/qemu-uboot-smoke.sh --net user`
2. Build Pi 4 payload with 26a manifest defaults:
- `scripts/pi4-image-build.sh --manifest configs/root_task_pi4_uboot_aarch64.toml`
3. Boot on Pi 4 and verify runtime lines include:
- `manifest.hw.network.enabled=true`
- `manifest.hw.network.backend=bcmgenet-v5`
- `manifest.hw.network.static_ipv4.ip=<configured-ip>`
- `manifest.hw.networking=enabled-static-ipv4`
4. Validate TCP console reachability from host:
- `cargo run -p cohsh --features tcp -- --transport tcp --host <STATIC_IP> --port 31337 --script scripts/cohsh/boot_v0.coh`

## Milestone 26b Pi 4 network wizard checklist (as-built)
1. Build Pi 4 payload with the 26b policy-capable manifest:
- `scripts/pi4-image-build.sh --manifest configs/root_task_pi4_uboot_aarch64.toml`
2. At the U-Boot wizard, either:
- wait for `Continue with existing config` or `Boot with manifest defaults`, or
- choose `Configure networking` and walk the prompts:
  - `DHCP ON (automatic address)` or `DHCP OFF (static IPv4)`
  - `Wired Ethernet (GENET)` or `Wi-Fi (CYW43455)`
  - when `DHCP OFF`, enter `Static IPv4 address`, `Prefix length`, and optional `Gateway IPv4`
- when `Wi-Fi`, enter `Wi-Fi SSID` and optional `Wi-Fi PSK`
- rely on the runtime `[local-seat] ...` xHCI ownership breadcrumbs for current Pi 4 keyboard evidence; U-Boot contributes only the menu USB session by default, while the final stop-state handoff contract is an explicit staging-time diagnostic option
- when firmware hands VL805 to root-task in a halted interrupt-quiesced stop state, runtime USB init distinguishes real acknowledged mailbox reset, bootloader post-stop authority, and weaker unconfirmed/post fallback states. A real ACK may still use the resetless stop-state lane, but the stopped seed remains poll-safe until independent live BAR/COMMAND authority exists; bootloader post-stop authority no longer publishes Cohesix rings by itself, while posted-fallback/soft-continue remain on the no-touch `bootloader-owned-pollsafe` diagnostic lane.
- if the trusted runtime handoff still freezes during the first controller ownership write, the exact-issue surface now reports the specific wedged store (`pre-run-config-store-wedged`, `pre-run-dcbaap-low-store-wedged`, or a later ring/ERST edge) rather than collapsing all failures into a generic controller edge.
3. Optional persistence:
- choose `Save current settings and reboot` or `Save settings and reboot` to persist only the Cohesix policy fields into `cohesix.env`.
4. Boot and confirm policy evidence:
- `[net-policy] source=manifest ...`, `[net-policy] source=dtb ...`, or `[net-policy] source=dtb rejected reason=<reason> ...`
- `manifest.hw.network.mode=static`
- `manifest.hw.network.interface=wired`
- `manifest.hw.networking=enabled-static-ipv4`
- for the manifest-default boot, `[net-console] init: bringing up backend=... mode=static interface=wired ip=192.168.10.42/24 ...`
- for wizard-selected `mode=dhcp`, `[net-console] pending-dhcp ...` followed by `[dhcp] lease bound ...` and then `[net-console] ready ip=<lease-ip> ...`
- for wizard-selected `mode=static`, `[net-console] init: bringing up backend=... mode=static interface=... ip=<static-ip>/<prefix> ...`
- if the staged DTB carries an explicit invalid override such as `wifi-psk-too-short`, root-task now rejects that override, emits `[net-policy] source=dtb rejected reason=<reason> ...`, and disables net-console with `invalid net config: dtb override rejected (<reason>)` instead of silently falling back to the manifest-selected backend.
- when saved wizard settings are used after reboot, root-task must not print `[boot] dtb locate skipped/failed: bootinfo extra truncated`; that message indicates the DTB handoff was lost before policy resolution and is a regression.
- when net-console never comes online, the serial/local-seat `nettest` and `netstats` commands must no longer collapse that state to a bare `detail=net-disabled`; they now preserve the boot-time init detail as `detail=net-disabled cause=<net-console init detail>`, so a failed CYW43 bring-up can still be diagnosed after the shell is live.

### Minimal next Pi 4 capture
1. Boot normally and do not run any debug commands until the first `cohesix>` prompt appears.
2. At the prompt, run `wifi dump-state`.
3. Run `usb status`.
4. Run `usb diag` to capture status and xHCI preflight without entering the live MMIO probe path or enabling background keyboard polling.
5. Run `wifi diag` to capture pre-HT state, a bounded HT probe, and post-probe state without retrying firmware upload.
6. If `wifi diag` shows HT ready but Wi-Fi is still down, run `wifi load-fw` once and then `wifi dump-state`; use `wifi retry` only when the dump shows a stale/failed transport state that needs the full retry ladder.
7. Normalize the raw serial log before comparing against older boots:
- `mkdir -p out/pi4-traces`
- `python3 scripts/pi4_trace_normalize.py ~/pi4-serial.log --summary > out/pi4-traces/cohesix-summary.json`
- `python3 scripts/pi4_trace_normalize.py ~/pi4-serial.log --domain usb > out/pi4-traces/cohesix-usb.jsonl`
- `python3 scripts/pi4_trace_normalize.py ~/pi4-serial.log --domain wifi > out/pi4-traces/cohesix-wifi.jsonl`
The normalizer is intentionally run after capture instead of from `scripts/pi4-image-build.sh`, so repeated stage-only builds do not overwrite or reinterpret hardware evidence.
8. For a Linux known-good comparison boot, copy the staged `brcmfmac-dyndbg.cmdline` into the Linux kernel command line or run `brcmfmac-dyndbg.sh` after boot, capture `dmesg -w`, and normalize it with `python3 scripts/pi4_trace_normalize.py <linux-log> --domain wifi --summary`.

Capture only these operator-facing lines from that session:
- `wifi: contract current=... expected=... observed=... blocker=... path=...`
- `wifi: sdhci_contract current=... preserved=... resolved=...`
- `wifi: sdhci_live cmd=... arg=... ps=... stat=...`
- `wifi: sdhci_preserved cmd=... arg=... ps=... stat=...`
- `wifi: cccr ioex=... iordy=... ien=... rframe_lo=... rframe_hi=... watermark=... devctl=... mesbusy=...`
- `wifi: sdio_shadow block_size_count=... transfer_mode=... backplane_bytes=...`
- `wifi: preserved_failure source=... stage=... exact=... sdhci=... f2_state=...`
- `wifi: f2_state=... exact_error=... sdhci_read_diag=...`
- `wifi: verdict=... focus=... bootstrap=...`
- `usb: ownership_proof live_cfg=... live_cmd=... cmd=... mailbox=... bar0=... publish_guard=...`
- `usb: contract current=... expected=... blocker=... touch_policy=... strategy=... seed=... publish_guard=... diag_fresh=...`
- `usb: enum_state phase=... outcome=... port=... connected_mask=... next=...`
- `usb: diag_contract stage=... diag_fresh=... tag=... exact=...`
- `usb: diag_values ...`
- `usb: verdict=... focus=...`
- `usb: xhci stage=... tag=... exact=...`
- `usb: xhci values ...`
- `usb: xhci_recent total=... count=... focus=dcbaap-run` plus any following `usb: xhci_recent[...] ...` lines
- `usb: ports cached=... count=... connected_mask=...` plus any following cached `usb: port... portsc=...` lines
- any `[local-seat] xhci diag summary ...` line that appears during `usb probe-kbd`

- when Wi-Fi bring-up fails, the serial log must now identify the failing stage with `[cyw43] step: ...` and `[pi4-wifi] ...` breadcrumbs before the final `mailbox-*`, `sdio-*`, or `cyw43-*` error detail. In particular, firmware load now emits `firmware core-ctrl mode=cmd53-windowed-read32-cmd53-byte-current-window fallback=cmd53-byte-rewindow split-window` before the ARMCR4/SOCRAM AI-core sequence so bring-up logs prove the primary mixed CMD53 backplane control path and the staged CMD53 byte-mode recovery path that root-task is using. Control-plane reply diagnostics now also emit `control-plane snapshot ... f2-window=0x18000000 f2-port=0x08000 read_addr_mode=fixed write_addr_mode=fixed ...`, and the corresponding `linux-f2-read-shape` / `linux-f2-write-shape` breadcrumbs include that same port/window tuple so Pi 4 traces prove that Function 2 traffic is using the Linux chipcommon FIFO shape instead of the older raw-zero path. Boot-time bring-up now also avoids unconditional long reset-deassert and SDIO retry spins: SDHCI power-on first emits `settle stage=sdhci-power-on mode=poll ...` and only falls back to the older long settle when the controller never reports ready, reset deassert emits `reset state=deasserted action=logical-only settle=skipped`, and the optional pre-reset HT assist plus the required final HT gate now share the short soft wait budget. The same path now also emits `backplane window program ... low=... mid=... high=...` plus bounded `sdio xfer chunk fn=1 ...` breadcrumbs so window-byte programming and per-chunk CMD53 address translation are explicit in the serial log, and same-window accesses now emit `backplane window reuse ...` instead of replaying the SBADDRLOW/MID/HIGH CMD52 triplet. Small CMD53 byte-mode transfers now drive SDHCI as a one-block transfer with block-count enable asserted, so a stalled `data-wait` on these logs indicates a transport failure rather than a half-programmed host byte-mode transfer. AI core-control writes no longer do a 32-bit read-modify-write before programming `AI_IOCTRL` or `AI_RESETCTRL`; the general `AI_IOCTRL` path still uses a direct byte-sized CMD52 write on the already-windowed unflagged Function 1 address, SOCRAM `assert-reset` emits `firmware core-ctrl reset-write mode=cmd53-word-windowed fallback=cmd52-byte-current-window-rewindow ...`, SOCRAM `clear-reset` now emits `firmware core-ctrl reset-write mode=cmd53-word-windowed fallback=cmd52-byte-current-window ...`, and the surrounding reset path now splits the first release edge into `stage=clear-reset-primary` and `stage=clear-reset-retry`. If that primary reset-release CMD53 write fails, root-task now recovers with `sdhci recover stage=core-ctrl-reset-clear-cmd52-current-window mask=cmd+data cache=preserved restored_window=... shadow_window=... fn=...` and retries only the current-window CMD52 path. The outer `clear-reset-write-retry` stage now skips a second `cmd53-word-windowed` replay and instead emits `stage=clear-reset-retry ... path=cmd52-byte-current-window cache=preserved` before retrying the preserved current-window write directly; a successful preserved retry still emits `firmware core-ctrl reset-clear stage=cmd52-current-window-ok ...`. If that SOCRAM retry-2 current-window write still fails with `sdhci-command-error`, root-task now emits `stage=clear-reset-retry-assumed-committed ... reason=socram-release-edge-timeout`, restores the cached backplane window from the shadowed `AI_RESETCTRL` access, and continues into the existing deferred-readback/postreset probe so the next breadcrumbs prove whether reset release actually committed before the transport dropped. The first SOCRAM post-release `AI_IOCTRL` write now emits `firmware core-ctrl postreset-write mode=cmd52-byte-current-window no-rewindow ...`; if that current-window write still returns `sdhci-command-error`, root-task now emits `stage=postreset-clock-en-assumed-committed ... reason=socram-postreset-write-timeout`, restores the cached backplane window from the shadowed `AI_IOCTRL` access, and continues into the readback path so the following breadcrumbs prove whether the post-reset `FGC|CLK` write committed before transport dropped. The first SOCRAM `postreset-clock-en-readback` `AI_IOCTRL` probe now keeps that same cached window alive across recovery, emitting `sdhci recover stage=core-ctrl-postreset-cmd52-current-window mask=cmd+data cache=preserved ...`, and stops after the direct current-window CMD52 read instead of immediately re-entering `SBADDRLOW` rewindowing, so the next remaining timeout identifies the actual post-reset read edge instead of a rewindow replay. If that direct current-window read still returns `sdhci-command-error`, root-task emits `stage=postreset-clock-en-read-deferred io=... reason=socram-fragile-postreset-read`, restores the cached backplane window from the shadowed `AI_IOCTRL` access again, and continues into the remaining readback/verify path so the next breadcrumbs prove whether the only remaining blocker is the subsequent `AI_RESETCTRL` read rather than the first post-reset `AI_IOCTRL` probe itself. That following SOCRAM `AI_RESETCTRL` confirmation read now also stays on the preserved-window/no-rewindow path; if it still returns `sdhci-command-error`, root-task emits `stage=postreset-reset-read-deferred reset=... reason=socram-fragile-postreset-reset-read`, restores the cached backplane window from the shadowed `AI_RESETCTRL` access again, and completes `verify` with the already-cleared reset value so the next remaining failure is beyond the post-reset reset-confirmation probe. After that SOCRAM `verify` stage, the first ChipCommon config write now also fences the exact handoff from the SOCRAM `AI_RESETCTRL` window into `firmware stage=chipcommon-config`: if the redundant `SBADDRLOW` replay times out while only the middle window byte actually changes, root-task emits `stage=chipcommon-config-retry ... reason=mid-byte-only-window-switch`, recovers SDHCI command/data state, rewrites only `SBADDRMID`, restores the cached target window with `stage=chipcommon-config-window-retarget`, and retries the first ChipCommon config write directly so the next remaining failure is beyond the initial post-reset ChipCommon window transition. The outer reset sequence still splits that edge into `stage=postreset-clock-en-write`, `stage=postreset-clock-en-write-ok`, and `stage=postreset-clock-en-readback` so logs distinguish the first post-reset write from the first post-reset readback while keeping the SOCRAM `AI_IOCTRL` edge on the direct byte path. Once a core is already held in reset the `in-reset-configure` `AI_IOCTRL` path now first checks whether the asserted-reset write would be redundant for SOCRAM, emitting `in-reset-configure-skip ... reason=redundant-after-assert` and reusing the pre-reset `FGC|CLK` state when the requested hold value matches the value already written before reset. When that SOCRAM skip path is taken immediately after a fresh reset assert, the next `AI_RESETCTRL` readback is also deferred and root-task emits `in-reset-ready-read-deferred ... reason=redundant-after-assert` instead of forcing another post-assert probe while Function 1 is still fragile. The SOCRAM `core_reset` path now also skips its redundant re-entry through `core_disable`, emitting `core-reset ... stage=skip-disable reason=held-reset-from-prior-disable`; if that path already established the same held-reset `FGC|CLK` value, root-task now emits `core-reset ... stage=pre-clear-in-reset-configure-skip ... reason=redundant-held-reset-from-prior-disable` and avoids replaying the same SOCRAM `AI_IOCTRL` write before `clear-reset`, so the next remaining failure can move to reset release itself instead of a redundant held-reset replay. ARMCR4 non-redundant in-reset `AI_IOCTRL` writes still emit `firmware core-ctrl in-reset-write mode=cmd53-word-windowed-in-reset ...` and first use the flagged word-sized backplane write path before falling back to CMD52 recovery, while SOCRAM held-reset `AI_IOCTRL` replays now emit `firmware core-ctrl in-reset-write mode=cmd52-byte-current-window fallback=cmd52-byte-rewindow ...` so the fragile SOCRAM prepare stage stays on the already-windowed byte path. The general access breadcrumbs still print both `bus=...` and `trace_bus=...` so the serial log shows the exact unflagged byte address used for live CMD52 writes and the flagged address used for window tracing. The AI core-control sequence otherwise follows the upstream Broadcom order: write `FGC|CLK` before asserting reset, re-apply `FGC|CLK` while the core is held in reset, then clear reset and leave `CLK` enabled; on Pi 4 SOCRAM, that explicit re-apply step is now skipped only when the immediately preceding `skip-disable` path already left the identical held-reset value in place. ARMCR4 uses the upstream `CPUHALT` bit during disable/release so the firmware CPU stays parked until the final release. If the primary word read path times out, root-task now first retries recovery against the current backplane window with CMD52 and only then falls back to a rewindowed CMD52 pass; reset-register writes now log stage-specific reset breadcrumbs before trying current-window CMD52 recovery, and the SOCRAM `clear-reset` path preserves the cached backplane window across that recovery instead of taking an immediate rewindow detour, while in-reset `AI_IOCTRL` attempts still log `write8-in-reset` fallback breadcrumbs before dropping back to CMD52 rewindow recovery. The serial log also emits `sdhci recover stage=cmd-wait|cmd-error|data-wait|data-error|finish-wait|finish-error mask=cmd+data` when host recovery is forced. The same recovery path now emits paired `sdio shadow ...` breadcrumbs that preserve the last programmed backplane window, Function 1 bus address, and cached chipclk/wake/sleep/cardcap control values without issuing extra probe traffic after a failure. On the pre-upload path, the Linux ALP-only pre-download request is the firmware-download gate; firmware upload starts once `ALP_AVAIL` is readable, and `HT_AVAIL` is only required later after firmware release. If the post-upload HT status bit still sticks low while ALP stays available, the log now emits `ht_contract ... blocker=ht-clock-before-function2`, caches `cyw43-ht-clock-timeout-before-function2`, and fails before Function 2 instead of entering the old forced-F2 fallback. The first firmware-channel mailbox `CMD52` edge remains part of bounded transport diagnostics only after HT has been proven, so a transient `sdio-cmd52-write` or `sdio-cmd52-read` during `setup-firmware-channel` or `wait-firmware-ready` emits the existing `action=recover-retry` breadcrumbs without weakening the HT-before-F2 gate.
- As-built correction: the long diagnostic paragraph above predates the current pre-firmware F2 policy. Root-task keeps SDIO Function 2 disabled before firmware/NVRAM upload, and a missing post-release `HT_AVAIL` blocks the later firmware-channel mailbox/control traffic in production boot. The forced ALP/KSO no-HT Function 2 path is diagnostic only.
- Current SR/KSO update: any older wording in this diagnostics paragraph that describes `SLEEPCSR.KSO` as pre-firmware-channel clock readiness is superseded. The Linux capture contains an SDIO Function 2 edge before `brcmf_sdio_download_firmware`, but Cohesix keeps F2 disabled before firmware upload because the seL4 Pi 4 path previously latched `IOEX=0x06` without `IOR2`. The later firmware-channel clock proof remains strict: production `wait-ht-clock` now primes `WAKEUPCTRL.HTWAIT` and CCCR `CARDCAP=CMD14_SUPPORT|CMD14_EXT`, preserves live `SLEEPCSR.KSO|DEVON` instead of running the clear/set ladder when the device-on bit is already proven, replays the Linux SD-only chip-clock fence with bounded settle, requests `CHIPCLKCSR.HT_AVAIL_REQ`, and stops production boot before Function 2 when `HT_AVAIL` or `IOR2` proof never appears. `SLEEPCSR.KSO`, DEVON, WAKEUPCTRL, and CARDCAP are sideband evidence at this phase; `FORCE_HT` remains reserved for post-proof/diagnostic paths and none of those sideband bits is accepted as strict firmware-channel readiness by itself.
- Wi-Fi SDIO transfer failures now emit a decoded CMD53 metadata line (`op/fn/addr/inc/blk/count/blksz/blkcnt/flagged/trn`) immediately before the failing `sdhci xfer error ...` breadcrumb. Finish-phase SDHCI failures also append the exact `err=sdhci-transfer-finish-*` label, so the observed `data-crc` loop surfaces as `sdhci-transfer-finish-data-crc` without leaving the existing SDHCI I/O-path fallback class. Firmware upload no longer lets the Function 2 diagnostic chunk clamp split ordinary Function 1 firmware windows into 512-byte byte-mode CMD53 writes; the expected first high-speed firmware write breadcrumb is a Linux-shaped 32 KiB Function 1 block transfer (`len=32768 blk=1 blksz=64 blkcnt=512 count=0 flagged=1`) because the SDIO CMD53 count field encodes 512 blocks as zero. The AI core-control path emits `firmware core-ctrl access ...` plus `firmware core-ctrl fallback ...` breadcrumbs that name the exact backplane window, flagged bus address, byte shift, value, and fallback direction used for each latched write/readback. Those lines are the primary evidence for distinguishing a CMD52 control-path failure from a CMD53 fallback failure.
- As-built correction: Pi 4 Wi-Fi firmware/NVRAM upload now follows Linux's `alp_only` pre-download clock contract. Root-task asserts KSO sideband, switches SDIO into the Linux high-speed lane with CCCR `SPEED.EHS` and SDHCI `HOST_CONTROL.HISPD`, then requests `CHIPCLKCSR.ALP_AVAIL_REQ` before upload. `ALP_AVAIL` is sufficient for firmware/NVRAM writes; a readable `HT_AVAIL` bit is recorded as stronger evidence but is not required until the post-release Function 2 clock gate. If `HT_AVAIL` is not visible, HAL still starts with the Linux-shaped high-speed 4-bit Function 1 block-transfer path using 32 KiB firmware chunks, then retries at 25 MHz and 12.5 MHz before the final 1.5625 MHz byte-mode fallback. HAL logs `firmware stage=pre-write-alp-clock ... mode=linux-alp-only-upload` and `firmware stage=write-firmware-bulk policy=conservative-alp-only` before firmware writes on the no-HT path, and fails before firmware writes only if ALP never appears.
- As-built correction: `WAKEUPCTRL.HTWAIT` and CCCR `CARDCAP=CMD14_SUPPORT|CMD14_EXT` are not pre-download prerequisites and do not replace the post-release HT proof. On the production post-release HT edge, Cohesix primes those sideband bits immediately before the strict HT request, preserves a live ready `SLEEPCSR.KSO|DEVON` state without clearing it to KSO-only first, and then issues the non-SR chip-clock off/request sequence before Function 2. Captured shapes such as `0x50`/`0x52` remain pre-firmware-channel blockers unless an explicit diagnostic no-HT probe is selected.
- As-built correction: before firmware upload, Pi 4 Wi-Fi now parks the D11 core in the same passive state used by the Broadcom/Linux activation path, then writes the staged firmware image, normalized NVRAM, and tail magic with the Linux-style memory order. The production boot path verifies the fully staged image/NVRAM/tail after NVRAM upload. Proven byte mismatches still fail before ARMCR4 release; transport-unavailable readback after all bounded conservative retry profiles logs `continue-before-armcr4-release` and releases ARMCR4 with `verified=no` so the next real HT or Function 2 gate is observable.
- As-built correction: after ARMCR4 release, the Wi-Fi firmware path skips the extra ARMCR4 readback probe, preserves the live post-release Function 1 sideband, clears `CHIPCLKCSR` to the Linux SD-only/off state with a bounded settle, and then requests `CHIPCLKCSR.HT_AVAIL` before firmware-channel Function 2 traffic. The SDIO-core `intstatus=0xffffffff` clear remains in the pre-release firmware activation step with the reset-vector handoff. After firmware upload restores the host clock, root-task restores the selected successful firmware-upload clock for the post-download F2 sequence instead of demoting back to the 400 kHz startup lane, and emits `post-release-proof` breadcrumbs that summarize the reset vector, upload/current clocks, and cached sideband state without adding new backplane reads. A KSO-only state is wake evidence only and does not advance production boot to firmware-channel traffic.
- As-built correction: the current Wi-Fi HT path supersedes the earlier optional-assist and bounded-retry wording. Pre-reset HT assist and pre-release recovery refreshes are KSO-only and preserve `CHIPCLKCSR`; the probe-attach KSO phase mirrors upstream `brcmf_sdio_kso_init()` and does not require DEVON before firmware upload. Cohesix leaves SDIO Function 2 disabled before firmware upload, while the pre-upload clock request follows Linux's ALP-only firmware-download edge. The production post-release clock gate preserves a live `SLEEPCSR.KSO|DEVON` sideband when already present, otherwise runs the bounded DEVON clear/set and KSO poll; if the live sideband remains KSO-only, production treats it as wake evidence, emits the SD-only/off `CHIPCLKCSR` fence, and issues the real `HT_AVAIL_REQ` so the next blocker is the HT clock proof rather than DEVON sideband. Function 2 traffic still requires real `HT_AVAIL` plus readiness. If the HT pass lands on the Pi 4 `0x50` timeout shape after that sideband, production boot keeps Function 2 disabled and fails only after the full bounded window instead of adding `FORCE_HT`, running a second clock ladder, taking an extra timeout shadow, or entering no-HT traffic.
- As-built correction: if the post-release forced ALP/KSO tuple is complete but Function 2 latches as `IOEX.F2` without `IORX.F2`, that is diagnostic evidence only. The strict production path treats `IORX.F2` plus real HT proof as the SDIO readiness contract; any no-HT Function 2 FIFO/mailbox probe must be explicitly diagnostic, bounded, and separate from normal boot.
- The former bounded no-HT startup-link rescue lane is diagnostic only under the current production gate. If a diagnostic snapshot still resolves to `cyw43-control-plane-sideband-*` or `cyw43-function2-enable-latched-not-ready-sideband-*`, HAL preserves that exact blocker and keeps the rescue budget bounded, but production boot does not re-enter Function 2 without real HT/IOR2 proof. The driver-side ioctl wait ladder remains shortened to `64_000 / 16_000 / 4_000` loops for initial / rescue / repeat rescue startup-link waits so the same first-reply blocker surfaces quickly instead of replaying Linux-sized control-response timeouts. The operator-facing `wifi dump-state` line-up now also includes `snapshot source=... stage=... rescue=... passive_limit=... replay_full_bootstrap=...`, so a single console capture shows whether the dump is live or cached, how much diagnostic rescue budget is already spent, and whether the current exact error would still trigger a full driver/bootstrap replay. When the net stack is present, the same command also emits `wifi: net backend=... mode=... policy=... active=... standby=... address_source=... dhcp_phase=...`, keeping the console useful after the first Function 2 reply when the remaining blockers are CLM/country/join or DHCP association rather than raw SDIO transport.
- On the USB preserve-state lane, `ERSTBA` now mirrors the earlier `ERSTSZ` no-touch seed logic: when the bootloader-exported halted seed already matches the desired single-entry table, runtime emits `0x0310` / `erstba-publish-skip-preserve` and skips the redundant low/high `ERSTBA` store pair.
- The first control-plane ioctl timeout on the startup-link probe lane is now treated as the preserved blocker if the cached exact error already names the same first-reply fault. That keeps `nettest` from collapsing the first timeout to a generic `cyw43 protocol error: ioctl-timeout`, and it stops the driver from re-entering the same long resend path once the exact blocker has already been observed. The bounded driver-side ioctl wait ladder is now `32000 / 8000 / 2000` loops for first startup-link / rescue / repeat-rescue waits, and once the final bounded probe is armed with `probe_pending=no` it collapses again to a `1000`-loop last wait instead of burning another full startup-link timeout.
5. Validate diagnostics surfaces:
- on the manifest-default boot, `netstats` includes `mode=static policy=wired active=wired standby=none addr_src=manifest-static ip=192.168.10.42 gateway=192.168.10.1 dhcp=disabled`
- on the manifest-default boot, `netstatus` prints `ip=192.168.10.42 gateway=192.168.10.1 src=manifest-static dhcp=disabled`
- on DHCP boots, `netstatus` prints the compact lease state (`ip=<lease-ip> gateway=<gw> src=dhcp-lease dhcp=bound`) so wrapped serial consoles still show the active address.
- `nettest` targets the active wired address only.
6. Current limitation:
- explicit `policy=wifi` now supports both `dhcp` and `static` when credentials are present.
- `auto` remains DHCP-only and still tries Wi-Fi first when credentials are present, but Milestone 26b is not complete until on-device Pi 4 serial logs prove join + DHCP and `auto` fallback behavior.

## macOS U-Boot debug harness (fast iteration)
- Purpose: validate U-Boot scripts/env/network setup behavior quickly before hardware retest.
- Build QEMU U-Boot:
`make -C third_party/u-boot qemu_arm64_defconfig`
`make -C third_party/u-boot CROSS_COMPILE=aarch64-linux-gnu- -j$(sysctl -n hw.ncpu)`
- Run:
`qemu-system-aarch64 -machine virt -cpu cortex-a57 -m 2048 -nographic -bios third_party/u-boot/u-boot.bin`
- In harness, test env/network primitives (`printenv`, `setenv`, `dhcp`, `ping`, `tftpboot`) and script logic.
- Limitation: this harness does not prove Pi 4 USB keyboard, HDMI output, or GENET fidelity; those are hardware-only checks.

## Milestone 26 boot evidence requirements
- `/proc/boot` must include:
- `manifest.sha256=...`
- Milestone 26 baseline: `manifest.hw.no_nic=true` and `manifest.hw.networking=disabled-m26-baseline`
- Milestone 26a networking: `manifest.hw.network.enabled=true`, `manifest.hw.network.backend=bcmgenet-v5`, `manifest.hw.networking=enabled-static-ipv4`
- `attestation.bound_manifest_sha256=...`
- `attestation.evidence_sha256=...` (when attestation enabled)
- Keep before/after proof for the same profile family:
- baseline no-NIC transcript (Milestone 26),
- NIC-enabled transcript (Milestone 26a).
- Pi 4 local-seat proof must show:
- HDMI displays `cohesix>` prompt.
- USB keyboard input reaches the existing root-console parser.
- Typed commands produce visible responses on HDMI with deterministic ordering relative to serial.
- A stale firmware DT handoff with `xhci` marked `status = "disabled"` does not strand local-seat: root-task ignores that stale node as an active runtime source, rejects leaked DMA-alias values as non-MMIO, does not derive a VL805 ECAM/config aperture from the xHCI BAR, and emits concise candidate-decision breadcrumbs before rejecting an unsafe xHCI MMIO source. On the default Pi 4 path, runtime may use the captured Linux high BAR `0x600000000` as static layout evidence for Cohesix-owned cold start after a local HAL mapping is pinned. U-Boot halted stop-state authority is now opt-in via `COHESIX_PI4_UBOOT_XHCI_HANDOFF=1`.
- When the captured high BAR is selected from an explicit source or from the bounded Linux-capture layout witness, local-seat uses the known-good Linux VL805 capability layout as the high-BAR seed and keeps xHCI event progression poll-driven. The default cold-start source is Cohesix-owned: preseed may pin the captured VL805 BAR through HAL using the static capture, but that mapping is not bootloader authority. Runtime now follows an explicit ordered xHCI attempt ladder and emits it in the live probe breadcrumb before `usb-oxide` touches live operational state. On a verified captured high-BAR path with the staged stopped register seed, prompt-safe probing leads with `origin=reset-owned-stop-seed`, `handoff=none`, skips controller entry, and leaves the branch poll-safe instead of publishing fresh rings. Linux virtual IRQ 27 and Linux MSI virq 30 are never used as seL4 IRQ numbers. The unseeded `origin=live-runtime-default` fallback remains the next bounded diagnostic lane; it can reach the guarded xHCI constructor only after the HAL-owned BCM2711 EXT_CFG proof succeeds.
- The captured high BAR remains the Linux-proven VL805 CPU resource. The live BCM2711 EXT_CFG path is now the default fresh ownership proof, but only inside HAL: it masks/clears BCM2711 PCIe sources before selecting the endpoint, validates live link/RC status when available, validates the exact VL805 identity/class/BAR tuple, disables MSI, and reads back COMMAND with memory, bus-master, and INTx-disable bits set. Static capture remains a fallback layout witness only; even with a pinned high BAR and captured COMMAND shadow it does not promote to fresh `platform-reset-complete` publication. After the VL805 mailbox reset ACK, local-seat retries the HAL-owned BCM2711 EXT_CFG proof on the high-BAR candidate even if the stale pre-reset status did not prove link/RC readiness; absent exact live tuple and COMMAND/BAR proof, fresh ownership remains unproven, and `allow-runtime-publish` is reported only when the COMMAND source is live `runtime-mapped`. IRQ27 is the seL4 virtual-timer PPI, so it remains excluded from this proof and from the USB sink set. A stopped seed does not weaken that platform-reset contract. Missing live replay proof blocks controller entry and returns to the UART console before runtime ownership is published.
- The earlier preserved-controller deferred-ownership breadcrumbs remain available inside `usb-oxide` for generic regression work, but the active Pi 4 local-seat runtime no longer depends on them. The runtime accepts the captured high BAR only when a HAL-mapped/pinned window exists or another verified source is present, then uses the captured VL805 capability layout to avoid an unsafe boot-time live cap read; U-Boot stop-state evidence can seed the first Cohesix-owned attempt without becoming bootloader ownership authority, and any failed ownership attempt falls gracefully back to serial-only input with explicit `[local-seat] ... unavailable` breadcrumbs.
- The Pi 4 U-Boot script no longer performs a forced handoff reprobe or emits USB trace breadcrumbs. It starts USB only for setup-menu input, then on boot clears stale `coh_xhci_*` trust tokens and either stops the active U-Boot USB host session or records that no session was active. By default it exports only the stopped xHCI register seed (`cohesix,xhci-usbcmd=0`, `cohesix,xhci-usbsts=1`, `cohesix,xhci-iman0=0`) without a high-BAR or trust-token ownership contract in both cases, so root-task can avoid unsafe early live `USBCMD`/`USBSTS` reads while still treating controller ownership as Cohesix cold start. `COHESIX_PI4_UBOOT_XHCI_HANDOFF=1` additionally exports the diagnostic high-BAR contract: `cohesix,xhci-mmio`, `cohesix,xhci-pci-cmd`, the same stopped register seed, and the four trust booleans. Root-task treats those final trust booleans plus a stop-state register snapshot as bootloader reset authority only on the opt-in diagnostic path; default capability layout comes from Cohesix-owned high-BAR evidence and runtime rings are rebuilt locally.
- If firmware DT hands off an `xhci` node with `status = "disabled"`, local-seat must treat that `reg` as stale for runtime safety, ignore it as an active xHCI source, and either use a verified runtime source or emit explicit degraded/unavailable diagnostics instead of cycling between blind legacy aliases.
- Pi 4 firmware DT `xhci` `reg` values authored in the BCM2711 `0x7e...` SoC bus window must be translated through the DT `ranges` mapping into CPU physical addresses before local-seat runtime candidate selection. Boot breadcrumbs must make that translation explicit.
- Pi 4 U-Boot USB proof for common keyboards must show that downstream hubs do not get stuck in repeated EP0 halts during early hub-class port control: per-port `PORT_POWER` is deferred for unsafe downstream hubs, Apple `05ac:1006` still gets its 250 ms post-config settle, and delayed child `status-error` retries do not issue extra `PORT_POWER` writes.
- Pi 4 USB local-seat DMA buffers must remain below `0xC0000000` (first 3 GiB), matching the BCM2711 PCIe `dma-ranges` limit used by VL805/xHCI. Linux exposes those same low RAM pages to VL805 as PCIe DMA bus addresses in the `0x4_XXXXXXXX` range (`dma-ranges` child bus `0x00000004_00000000` to parent `0x00000000_00000000`, size `0x00000000_c0000000`), and Linux xHCI debugfs shows runtime rings such as `DCBAAP=0x00000004_40d33000`. Cohesix therefore publishes `0x4_00000000 + phys` for VL805 xHCI DMA pointers while still allocating the backing pages below the 3 GiB ceiling.
- On the trusted Pi 4 xHCI handoff path, root-task routes any runtime-owned DCBAA, scratchpad, command-ring, and ERST/event-ring buffers through the audited DMA share/cache-maintenance path before publication. The stop-state lanes still do not publish those buffers: `DCBAA[0]`, `DCBAAP`, `CRCR`, `ERST*`, `ERDP`, `DNCTRL`, and `USBCMD.RUN` remain untouched until stronger runtime ownership evidence exists. Mailbox reset alone is not enough to rebuild fresh runtime rings from Cohesix-owned DMA buffers; the default fresh path also requires the HAL-owned BCM2711 EXT_CFG BAR/COMMAND proof. U-Boot no longer mirrors pre-handoff ring state into `/chosen/cohesix,*`; when the stop-state seed has no runtime-ring pointers, probing stays poll-safe before any unseeded live-default fallback.
- The current Pi 4 xHCI recovery path treats the old bootloader-owned stop-state lane as unsafe for live root-port sampling and unsafe for firmware-owned ring preservation. The Linux-derived path uses the captured high BAR and capability layout only after an explicit source exists; fresh Cohesix-owned rings and VL805 PCIe DMA bus aliases belong to an enumerating ownership lane, not to a stopped seed alone. When the staged stopped seed is present, the non-authoritative seed suppresses the known-toxic first live `USBCMD`/`USBSTS` reads and enters `origin=reset-owned-stop-seed` first, but that lane now remains poll-safe and skips HCRST, CONFIG, fresh `DCBAAP`, ring, interrupt, root-port, and `RUN` ownership touches. The seeded cold-start lane still emits `xhci probe skipped ... reason=bootloader-owned-no-fresh-ownership action=return-to-shell` and records `enumeration-disabled-bootloader-owned` when runtime falls back to diagnostic evidence rather than an enumeration-capable result. Genuinely unconfirmed mailbox states still fail back to serial-only input.
- The operator-facing `usb probe-kbd` summary now also emits an `irq_contract` line showing the poll-only runtime contract plus any remaining controller gate. That means a single serial capture now distinguishes “controller never entered xHCI because of a policy/ownership gate” from “controller entered xHCI and failed at diag stage X” without needing the raw `[local-seat]` breadcrumbs.
- The same preserve-state contract now keeps `DNCTRL` fully bootloader-owned on the trusted stop-state lane: runtime emits `0x0314` / `dnctrl-write-skip-preserve` and skips both the old pre-`RUN` zero and any post-`RUN` `dnctrl-post-run` touch on that degraded IRQ27 preserve path.
- The preserve-state DCBAAP ladder now skips replaying the staged current snapshot value on the trusted bootloader-handoff path and goes straight to the real desired publish instead, because that self-write was the first remaining preserve-state edge to wedge under degraded IRQ 27.
- As-built correction: the forced no-handoff xHCI path no longer treats the Pi 4 Linux-captured VL805 capability tuple as boot authority by itself. Boot-time runtime uses the capture as a layout witness after HAL/pinning has a high-BAR window, and when the staged stopped seed is present it leads with the non-authoritative `handoff=none` / `origin=reset-owned-stop-seed` lane. The unseeded live-default lane remains available only when no stopped seed exists or as an explicit trusted diagnostic fallback.
- As-built correction: Pi 4 Wi-Fi firmware staging now prefers the known-good Linux-captured `cyfmac43455-sdio.bin` and `cyfmac43455-sdio.clm_blob` assets plus the matching Raspberry Pi 4 NVRAM file. A correct next boot should show `firmware stage=write-firmware bytes=609309`; a repeated `bytes=631467` means the old Cohesix firmware bundle is still being embedded.
- As-built correction: Pi 4 Wi-Fi NVRAM staging now strips the raw Raspberry Pi text file with the Linux `brcmf_fw_nvram_strip()` shape before upload: comments, blank lines, carriage returns, invalid keys, and `RAW1` records are removed; each remaining `key=value` entry is NUL-separated; `boardrev=0xff` is added only if absent; and the Broadcom length token stays as the separate tail word. With the known-good capture bundle, a correct next boot should show `firmware stage=write-nvram bytes=1744 offset=0x0025f92c` followed by `firmware stage=write-nvram-tail magic=0xfe4b01b4`; `bytes=2076 offset=0x0025f7e0` means raw NVRAM text is still being uploaded.
- As-built refinement: on the degraded IRQ27 preserve-state fallback, runtime now seeds the preserve-state `ERSTSZ` publish from the desired single-entry table itself instead of re-reading or replaying a stale zero-valued stop-state seed. When that preserve-state publish is already redundant, local-seat emits `xhci.diag stage=0x02da tag=erstsz-publish-skip-preserve` and skips the `ERSTSZ` store entirely, so the next halt identifies the next real deferred ownership edge rather than another redundant preserve-state size touch. The same no-touch rule now applies to deferred `ERDP` too: if the trusted halted queue pointer already matches the desired dequeue pointer, local-seat emits `xhci.diag stage=0x0311 tag=erdp-publish-skip-preserve` and leaves the live `ERDP` low/high pair untouched. The operator-facing `usb status` / `usb probe-kbd` summary now also carries `pathway=` and `diag_fresh=yes|no`, so a single console capture shows whether the reported xHCI stage belongs to the active probe pathway or only inherited stale route state.
- On the Wi-Fi side, the former bounded no-HT startup-link recovery is no longer part of production boot. Any `startup-link-resume` / `reply-probe-bypass` Function 2 ready-probe remains diagnostic and bounded; production boot fails before Function 2 when `HT_AVAIL` and `IOR2` are not both proven. The first speculative reply-read clock-borrow rules apply only after a real Function 2 reply path exists.
- As-built refinement: boosted startup-link reply probes now choose their diagnostic prefix shape from the effective reply-read clock, not the pre-boost transport clock. When the bounded startup-link lane is about to borrow the Linux-style `12.5 MHz` reply clock, unstable Function 1 hints no longer collapse that probe back to the 8-byte pure-F2 prefix; the failing Function 2 read now stays on the Linux `BRCMF_FIRSTREAD`-sized prefix while still avoiding the stale sideband hints that triggered the low-touch lane in the first place.
- As-built refinement: the `linux-rxfail` Function 2 reply-recovery lane now mirrors Linux `brcmf_sdio_rxfail()` more closely. After the existing `abort-f2` + `rf-term` drain pass, root-task now also posts `SMB_NAK` to `tosbmailbox`, so the CYW43 firmware is explicitly asked to retransmit the stalled first control-plane reply instead of leaving the startup-link retry path dependent on the unread frame already in flight.
- As-built refinement: strict Wi-Fi reply recovery now treats `cyw43-control-plane-sideband-read-stall-no-buffer-ready` as the same startup-link blocker class as the earlier sideband-unreadable case. Diagnostic no-HT reply probes may still label the same blocker explicitly, and `wifi dump-state` no longer lets a cached generic ready-state snapshot hide that live sideband-read-stall edge.
- As-built refinement: startup-link timeout recovery now also treats `mode=startup-link-resume` as part of the same first-reply rescue ladder, so a second-phase `sdio-function2-ready-timeout` still reports `blocker=first-control-plane-reply` instead of falling out of the startup-link path early. The operator-facing `wifi dump-state` verdict follows that same interpretation and now classifies the stalled/blank-exact startup-link-resume case as `function2-reply-edge` / `first-function2-reply`.
- As-built refinement: the bounded CYW43 driver/bootstrap retry gates now follow the same first-reply contract as the HAL snapshots. `cyw43-function2-enable-latched-not-ready-sideband-*`, direct `cyw43-function2-reply-*`, generic `cyw43-control-plane-sideband-*`, `cyw43-control-plane-no-reply-linux-f2-armed`, and the bounded pure-F2 wrapper `cyw43-control-plane-pure-f2-startup-link-no-reply` all stay on the startup-link first-reply rescue ladder, preserve the promoted resend clock when appropriate, and remain eligible for the bounded bootstrap retry ladder instead of collapsing immediately into the terminal net-disabled policy result. The top-level firmware/bootstrap replay gate now stays closed for that entire first-reply family, so `init_control_plane` preserves the real blocker in `dump-state` / `nettest` instead of replaying the same long bootstrap again; `cyw43-control-plane-startup-link-rescue-budget-exhausted` still stays terminal when rescue is exhausted. When the driver later proves a narrower direct `cyw43-function2-reply-*` blocker than the last cached HAL snapshot, root-task now promotes that direct blocker back into the cached snapshot too, so `wifi dump-state` and `nettest` report the same first-reply edge instead of splitting across direct-reply vs sideband wording.
- As-built refinement: the conservative preserve-state branch no longer uses the old deferred `CRCR` ownership shortcut. That branch still seeds `DCBAAP`/`CRCR`/`ERST*`/`ERDP` from zero or the bounded stop-state snapshot only, takes the same bounded post-mailbox-reset blind settle before runtime MMIO ownership begins, skips the first live `USBCMD.HCRST` / `CONFIG` touch under `mode=preserve-controller-state`, and now completes the staged preserve-state event-ring table plus `DCBAAP` and `CRCR` before `USBCMD.RUN` while keeping the first live `DNCTRL` quiesce store on the deferred post-`RUN` ladder. On the trusted preserve-state lane, the current `CRCR` publish is now treated the same way as the other no-touch ring registers: if the bootloader-exported halted seed already matches the desired command-ring pointer, runtime emits `0x0313` / `crcr-publish-skip-preserve` and skips the redundant low/high `CRCR` store pair instead of replaying the staged current value into the controller. That keeps the remaining startup fault isolated to the next real controller-visible edge instead of letting a preserve-state `CRCR` replay wedge the lane before `RUN`. On the active Pi 4 stop-state preserve branch, the first live `USBCMD.RUN` edge now uses the same U-Boot-style direct `writel()` path as the known-good upstream `xhci_start()` sequence rather than the earlier release-only helper, so the `xhci.diag` transition from `0x02e9 usbcmd-run-pre-store` to the matching post-store breadcrumb still cleanly distinguishes "the fence/drain wedged VL805" from "the RUN store itself wedged VL805". The abandoned stop-state `CRCR` handoff read is gone; any remaining `xhci.diag stage=0x0253 tag=crcr-read-begin` breadcrumb is now part of the runtime-owned path rather than the earlier stop-state shortcut.
- Pi 4 Wi-Fi AI core-control diagnostics still surface both sides of the staged recovery path: flagged CMD53 windowed reads, byte-sized CMD52 current-window writes, preserved-window SOCRAM clear-reset retries, and the paired `sdhci recover ...` / `sdio shadow ...` breadcrumbs that make transport failures attributable. The active post-upload transport contract remains strict: root-task records cached KSO/DEVON sideband state, uses production `wait-ht-clock` to prime wake/CMD14 sideband while preserving an already-ready `SLEEPCSR=0x03`, requests `CHIPCLKCSR.HT_AVAIL`, and fails before production Function 2 when `HT_AVAIL` or `IOR2` is missing. The Pi 4 `0x52` forced ALP/KSO tuple is diagnostic evidence only. Failure snapshots still end with compact control-plane diagnostics when the path reaches reply-side traffic.
- The initial `setup-firmware-channel` block and the later control-plane rearm block remain transport-dependent, but the bounded no-HT firmware-channel lane is disabled. On the strict path HAL uses the bounded startup-clock write window, restores the faster link only after mailbox / interrupt / device-control writes complete, and logs `[cyw43] control-plane reply wait rearm policy=promoted-link|startup-link ...` so a serial capture still shows whether a later reply failure happened on the promoted link or deliberate startup-link retry.
- On the bootloader-owned stop-state-only xHCI snapshot branch, runtime still uses the trusted CAP layout plus stop-state diagnostics, suppresses the early halt revalidation touch, stays on the seeded `mode=cold-start-from-snapshot` ladder, and still skips the first runtime `USBCMD.HCRST` / `CONFIG` edge (`xhci.diag stage=0x0234` / `0x0245`). The latest Pi 4 traces moved the remaining failure through post-ready `ERSTSZ` and then to the first live pre-`RUN` `ERDP` low-word store, so the weak stop-state lane no longer publishes fresh `DCBAAP`, `CRCR`, `DNCTRL`, or event-ring state at all. The old stop-state deferred `CRCR` read remains gone; any remaining `xhci.diag stage=0x0253 tag=crcr-read-begin` breadcrumb is now part of a runtime-owned path instead.
- When Pi 4 USB keyboard bring-up terminates in `detail=xhci-init`, local-seat also emits `xhci diag summary context=keyboard-init stage=... tag=... a=... b=... c=...` so the last xHCI state survives diag rate limiting and can be matched directly against the earlier `xhci.diag` ladder.
- As-built safety rule: once the UART root shell is live, the event pump no longer auto-probes the Pi 4 USB keyboard path on its own. Local-seat keeps HDMI mirroring active when framebuffer setup succeeds; if mailbox/framebuffer setup fails, local-seat degrades to headless mode and keeps the USB diagnostic backend available instead of returning `local-seat-unavailable`. Backend keyboard polling stays deferred with `runtime keyboard poll deferred action=serial-shell-first` until an explicit runtime action enables it, so a toxic xHCI probe cannot wedge the board after boot has already succeeded.
- The explicit runtime action is the serial/local-seat `usb` debug surface: `usb status` reports whether the local-seat backend is attached and whether runtime keyboard polling is still deferred, `usb enable-kbd` arms deferred runtime probing, and `usb probe-kbd` immediately runs one on-demand keyboard probe pass after arming polling. `usb probe-kbd` now emits a three-line `golden_path preflight ...` block before xHCI MMIO starts, so even a halt before return still leaves the chosen origin/handoff/seed/halt-guard, the first expected diagnostic edge, and the planned happy-path shape (`ctor`, `pre`, `legacy`, `run`, `publish`, `post_ready`) in the serial transcript. The same serial-local debug surface continues to expose `wifi dump-state`, `wifi probe-ht`, `wifi load-fw`, and `wifi retry` for staged CYW43455 bring-up.
- After firmware/NVRAM staging, ARMCR4 clear-reset and post-reset single-byte proof reads skip the fragile CMD53 data-phase ladder and begin on the current-window CMD52 byte path: the serial log should now show `firmware core-ctrl access op=read8-clear-reset mode=cmd52-byte-current-window retry=cmd52-byte-rewindow ...` for the mandatory reset-release proof and `firmware core-ctrl access op=read8 mode=cmd52-byte-current-window retry=cmd52-byte-rewindow ...` for `postreset-clock-en-readback` and `armcr4-core-up` readbacks. A direct `sdio-cmd52-read` failure is treated as the same bounded fragile-postreset-read class as the older command/data timeout cases. The expected follow-up is therefore either a real `clear-reset-ready` / `ready io=... reset=...`, a bounded `readback-retry ... reason=armcr4-fragile-postreset-read`, or the existing deferred `postreset-clock-en-read-deferred ...` / `readback-deferred ... reason=armcr4-fragile-postreset-read-assumed-core-up` verdict when the transport is still settling.
- The `wait-ht-clock` serial evidence is now decoded as well as timed. On the non-SR production post-download path, root-task primes `WAKEUPCTRL.HTWAIT` and CCCR `CARDCAP=CMD14_SUPPORT|CMD14_EXT`, preserves live `SLEEPCSR.KSO|DEVON` without the clear/set ladder when the read is already device-on, emits the Linux-style SD-only `CHIPCLKCSR=0` fence, then emits `active-ht-request=0x10` and logs decoded `status=active-ht-request-readback|active-ht-timeout ... ht_req=<y|n> alp_req=<y|n> force_ht=<y|n> clkreq_off=<y|n> alp=<y|n> ht=<y|n> wake=0x../<y|n> sleep=0x../<y|n> cardcap=0x../<y|n>`. Production `wait-ht-clock` must not emit a `post-download-sleepcsr-clear-set` when live `SLEEPCSR=0x03` is already proven, a pre-Function-2 `active-sr-kso-ready ... reason=skip-chipclk-ht-poll`, or a `force_ht=y` retry.
- The first SOCRAM reset-release path now adds `pre-clear-in-reset-configure ... reason=required-before-clear-reset`, but when `skip-disable` already established the same held-reset value it emits `pre-clear-in-reset-configure-skip ... reason=redundant-held-reset-from-prior-disable` and avoids replaying that identical write before `clear-reset-prewrite-delay ... reason=socram-fragile-first-write`. On failure it still makes a single `clear-reset-write-retry ... reason=socram-fragile-first-write` recovery attempt before the existing deferred `clear-reset-read-deferred ...` readback path, and that first clear-reset recovery now preserves the cached backplane window while skipping an immediate `cmd52-byte-rewindow` detour. The second attempt also now skips replaying the already-failed `cmd53-word-windowed` reset-clear write and instead retries the preserved `cmd52-byte-current-window` path directly, so logs distinguish a redundant held-reset replay from a transient reset-release edge stall instead of a self-inflicted reset-clear word-write replay.
- Pi 4 Wi-Fi firmware load now disables the pre-reset HT assist on the ARMCR4/SOCRAM reset-heavy path. Pre-HT recovery refreshes use the sideband-only helper, preserving `CHIPCLKCSR` and replaying only KSO before Function 2, so a later reset-edge fault is not hidden by a fresh `0x21` ALP-hold write or speculative SR register programming. The required post-firmware clock proof primes wake/CMD14 sideband on production `wait-ht-clock`, preserves live `SLEEPCSR.KSO|DEVON` instead of clearing it when it is already ready, replays Linux's SD-only chip-clock fence, requests `HT_AVAIL_REQ`, and waits for real `HT_AVAIL` before Function 2. Function 2 readiness still emits `sdio function-ready fn=2 action=prime-block-size ...` plus `sdio function2 ready-snapshot ... diagnosis=... corecontrol=... corestatus=... cc_f2rdy=...`, so the next failure log distinguishes a true `IOR2` latch failure from the `f2-ready-hidden-from-cccr` case where CCCR `IORx` stays low even though SDIO core `corecontrol` already shows `CC_F2RDY`. HAL still keeps the startup-link fallback as a guardrail only after the HT and Function 2 contracts have been met.
- ARMCR4 disable now treats the post-hold `AI_RESETCTRL` readback as advisory on the Pi 4 fragile Function 1 path. After the assert-reset write and in-reset `AI_IOCTRL` hold write both succeed, an `sdhci-int-timeout` or CMD52 read failure on the immediate `AI_RESETCTRL` proof emits `stage=in-reset-ready-read-deferred ... reason=armcr4-fragile-in-reset-reset-read`, resets the SDHCI command/data path, refreshes the cached KSO/cardcap/chip-clock transport state, assumes reset is held, and continues to the next Broadcom proof point instead of replaying more toxic CMD53 readbacks. The ARMCR4-only settle budget is shorter than the SOCRAM settle budget so this failing lane no longer burns the same delay reserved for SOCRAM reset release.
- The SOCRAM disable entry now follows the upstream Broadcom write-first path for the `prereset=0/reset=0` case: HAL emits `stage=entry-read-deferred ... reason=socram-upstream-write-first`, writes `AI_IOCTRL=0`, asserts reset, and defers the fragile entry read that previously moved the first post-ARMCR4 recovery failure to `0x18104408`.
- When strict reply recovery reaches the Linux-aligned `ioex=0x06` / `iorx=0x02` state but the reply-side Function 1 sideband registers remain unreadable, HAL treats that snapshot as a bounded first-reply diagnostic rather than proof of readiness. Production boot still requires real HT and `IOR2` before Function 2 traffic; pure-F2 startup-link reads are reserved for explicit diagnostics after a proven F2 latch. Those diagnostic reads stay fully aligned with the low-touch pure-F2 policy: HAL first pulls only the minimal 8-byte SDPCM prefix on Function 2, validates the header from that prefix, and only then requests the remainder of the reply instead of leading with the older Linux-sized 64-byte first-read on the fragile startup-link path. Once that minimal prefix read itself proves the same `cyw43-function2-reply-read-stall-no-buffer-ready` blocker, HAL stops there instead of escalating through larger 12/16/32/64-byte diagnostic prefixes that only repeat the same timeout more slowly. Failure snapshots report the bounded diagnostic cycle counts and preserve the exact first-reply blocker so `wifi dump-state` does not hide whether the remaining edge is sideband hints, Function 2 readiness, or direct Function 2 reply visibility.
- The operator-facing `wifi dump-state` summary now also emits a `reply_contract` line that classifies the active reply path (`startup-link-f2` vs sideband/strict path), whether strict recovery will preserve the proven Linux Function 2 latch or repoll `IOR2`, and the current blocker class (`direct-f2-reply`, `f1-sideband`, `f2-ready`, or `f2-interrupts`). That keeps the final reply-side frontier readable from one serial capture without reconstructing the policy from scattered breadcrumbs.
- Pi 4 xHCI bring-up now emits explicit pre-`RUN` decision breadcrumbs so the remaining USB stall can be classified without guessing: `xhci probe params ... axi_setup=<yes|no>` / `xhci probe begin ... axi_setup=<yes|no>` now confirm from the serial wrapper whether the Broadcom AXI vendor-register quirk will even run before control enters the xHCI constructor. Trusted firmware-handoff starts (`cold-start-from-snapshot`, `resetless-reinit`, `preserve-controller-state`) now leave `axi_setup=no` and reuse the bootloader-programmed AXI attribute state directly; the Pi 4 high-BAR path also leaves `axi_setup=no` for any bare fallback, and only a non-high generic-wrapper runtime-init path may keep `axi_setup=yes`. The active live-EXT_CFG-proven `platform-reset-complete` path relies on HAL ACKs for the PCIe host/child INTx sinks, skips the post-mailbox HCRST and CONFIG rewrite edges, and publishes fresh DCBAA/rings before `RUN`. The older `0x0340..0x034b` labels remain to decode captured DCBAAP store traces; `0x034c` remains a historical blocked-lane label rather than the expected active guard.
- As-built correction: xHCI init now emits `0x0117 tag=init-policy-summary` for the firmware-handoff/runtime-seed/publish policy tuple, leaving `0x0111` dedicated to the Broadcom AXIWRA vendor-register edge. Preflight expected diagnostics now also name the first actual post-policy breadcrumb for lanes that enter the controller: generic unseeded full pre-reset lanes expect `0x0200 tag=pre-reset-usbcmd-usbsts-read`, seeded skip-pre-reset lanes that are trusted enough to enter xHCI expect `0x0204 tag=fw-handoff-skip-pre-quiesce`, and the Linux-captured high-BAR live-default lane also expects `0x0204` because it deliberately skips the toxic initial live operational read. A reset-owned stop-state seed without runtime-ring ownership is now a policy-return lane instead: preflight reports no expected xHCI diagnostic stage and `followup=fallback-next` when a live fallback remains.
- As-built correction: generic unseeded Pi 4 cold-start constructors no longer write `USBCMD=0` unconditionally. Runtime first samples `USBCMD`; it emits `0x0209 tag=fw-handoff-skip-usbcmd-mask-write` and keeps the edge read-only when U-Boot already left interrupt delivery masked, and only emits `0x0205` when `INTE/HSEE` bits must actually be cleared. The Linux-captured high-BAR live-default path performs no initial live `USBCMD`/`USBSTS` read while staging the mailbox gate; after `mailbox-acked`, live BAR/COMMAND replay evidence, and bounded PCIe sink installation, the promoted `handoff=platform-reset-complete` lane enters xHCI construction, skips the second HCRST and CONFIG rewrite, publishes DCBAA/rings, and proceeds toward poll-driven controller-ready enumeration. Without live command proof, the mailbox-acked path still selects the platform policy, but local-seat blocks controller entry and returns to the prompt before any runtime ring publication.
- On trusted Pi 4 handoff lanes, `usb-oxide` now keeps post-ready interrupter cleanup disabled: cold-start, preserve-state, and runtime-owned stop-seed paths report `post_ready=irq-skip`, avoid direct `IMOD` / `IMAN` writes, avoid `IMAN.IP` pending clears, and do not clear `USBSTS.EINT` before software has consumed event-ring TRBs. Event-ring progression is poll-driven by command/transfer polling and advances `ERDP` only after a TRB is consumed.
- Pi 4 USB local-seat runtime no longer treats IRQ27 as part of the prompt-safe xHCI sink set; on this seL4 build IRQ27 is the kernel timer, while Linux's PCIe IRQ 27 is only a Linux virtual IRQ label. Before pre-controller ownership writes, the prompt-safe high-BAR lanes may bind the generated seL4/GIC PCIe host and child INTx-A IDs (`180` and `175`) through HAL as level-triggered sinks. They remain poll-driven for controller-ready enumeration.
- The controller-ready phase remains poll-driven on the active trusted lanes; root-port detection must proceed without attempting to acquire IRQ27 from the kernel timer owner.
- The root-console Wi-Fi diagnostics now format status lines at the full serial line capacity instead of a fixed 128-byte scratch buffer. That keeps long preserved first-reply blockers visible end-to-end in `wifi dump-state`, so a cached `cyw43-function2-enable-latched-not-ready-sideband-read-stall-no-buffer-ready` blocker now survives the console formatter instead of truncating the operator-facing line at `exact_error=`.
- On an explicit Pi 4 diagnostic no-HT startup-link path, the first hintless Function 2 reply probe remains eligible even after the temporary `12500000Hz` bulk-clock boost. Once sideband / mailbox hints have gone stale in diagnostics, startup-link can still issue the hintless first Function 2 read instead of deferring forever waiting for fresh frame hints that never arrive on the degraded first-reply stall.
- As-built refinement: the low-touch pure-F2 startup-link reply diagnostics now key off the live reply-rearm mode, not a stale cached mode field. On the diagnostic startup-link lane that keeps the prefix ladder pinned to the minimal 8-byte SDPCM prefix instead of silently expanding back out to 12/16/32/64-byte reads after the same first-reply stall is already proven.
- Pi 4 Wi‑Fi control-plane preinit now tracks Linux more closely once transport is alive: after `country`, root-task programs `mpc=1`, preserves and re-publishes the global `event_msgs` mask with `EVENT_IF` enabled, applies the Linux default scan channel / unassociated scan dwell times, and best-effort enables `txbf` before the existing bounded join/auth path continues. `wifi dump-state` now also prefers a cached preserved first-reply blocker whenever the live snapshot is still in `startup-link-recovery`, the Function 2 state is latched on the same first-reply edge, and the live `exact_error` has collapsed back to blank or generic no-reply text.
- Those cached Wi-Fi first-reply blockers are now monotonic across the same startup-link prefix-read edge. Once HAL has captured a specific Function 2 reply stall or sideband stall, weaker follow-on startup-link snapshots on that same `control-plane-reply-prefix-read` path no longer flatten `exact_error` back to blank, generic no-reply, or bare `cyw43-function2-enable-latched-not-ready`.
- As-built refinement: low-touch pure-F2 reply diagnostics now stay confined to the startup-safe `400000 Hz` lane. Once the bounded startup-link reply probe has borrowed `12500000 Hz`, the live Function 2 read path goes back to the Linux `BRCMF_FIRSTREAD`-sized first read instead of shrinking the real probe down to the 8-byte pure-F2 prefix shape.
- As-built refinement: when that same cached direct first-reply blocker survives a later pure-F2 follow-on snapshot, `wifi dump-state` now also keeps the stronger cached `f2_state=latched-linux-configured-no-iorx` instead of degrading it to `latched-no-iorx` just because the low-touch pure-F2 path intentionally suppressed fragile Function 1 sideband readbacks on that follow-on sample.
- As-built refinement: the same monotonic `f2_state` rule now also applies when the final cached failure snapshot keeps the exact same direct `cyw43-function2-reply-*` blocker. Later `cyw43-init-control-plane-fail` and `console-dump-state` snapshots on that same first-reply edge no longer overwrite an earlier proven `latched-linux-configured-no-iorx` state with `latched-no-iorx` just because the fallback path re-sampled through pure-F2-only diagnostics.
- As-built refinement: the diagnostic pure-F2 Wi-Fi rearm path treats an earlier cached direct first-reply blocker plus `latched-linux-configured-no-iorx` state as meaningful on the same startup-link edge. Once low-touch pure-F2 diagnostics have deliberately stopped trusting fragile Function 1 sideband reads, later diagnostic channel-rearm passes can preserve the already-proven Function 2 enable latch instead of needlessly forcing another Function 2 ready repoll just because the live sideband-derived `f2_state` degraded to `latched-no-iorx`.
- As-built refinement: that same cached `latched-linux-configured-no-iorx` Function 2 state now also survives diagnostic `startup-link-passive-wait` snapshots. Production boot still fails before Function 2 without strict HT/IOR2 proof, but cached diagnostics can preserve the already-proven Linux Function 2 latch instead of degrading the operator-facing state.
- As-built refinement: `wifi dump-state` now reports diagnostic passive startup-link reply frontiers with their effective reply probe clock. Once an explicit diagnostic path reaches `startup-link-passive-wait`, operator output shows the preferred reply clock rather than a stale transport clock, which keeps slow-fail diagnostics aligned with the actual first-reply probe path.
- As-built refinement: that cached Linux-configured Function 2 state now also feeds the low-touch pure-F2 snapshot builder itself. When `post-firmware-ready-function2-strict-repoll-soft-continue` and later bounded reply snapshots deliberately suppress fragile Function 1 sideband reads, they now keep the earlier proven `latched-linux-configured-no-iorx` state instead of downgrading cached operator output to `latched-no-iorx`.
- As-built refinement: `cyw43` now promotes a direct `cyw43-function2-reply-*` init failure into the cached HAL snapshot before it asks HAL for the `cyw43-init-control-plane-fail` debug dump. That keeps the final cached failure snapshot on the same direct first-reply blocker path that HAL already knows how to preserve, so `wifi dump-state` no longer ends up with a hybrid `exact_error=cyw43-function2-reply-*` plus a weaker `f2_state=latched-no-iorx`.
- As-built refinement: `debug_dump_state()` now applies the same cached first-reply blocker merge before deciding whether to return the cached snapshot. On the `wifi dump-state` console path that keeps the stronger preserved `f2_state` and `sdhci_read_diag` attached to the same direct `cyw43-function2-reply-*` blocker instead of letting the final live sample regress them just before operator output.
- As-built refinement: the trusted Pi 4 `uboot-fresh-init` xHCI cold-start retry lane now keeps constructor scrub completely write-free as well. On that path the `full-reset-start` route reports `ctor=trusted-quiesce`, emits `0x0209` followed by `0x020d` / `0x020e` / `0x020f`, and no longer touches `USBSTS` at `0x0206` before reset ownership is re-established.
- As-built correction: prompt-safe Pi 4 keyboard probes now treat the seed-backed reset-owned path as the first diagnostic path whenever a stopped seed is present. Manual `usb probe-kbd` preflight with bootloader post-stop authority should first report `route=trusted-high-bar-stop-seed-primary`, `policy=reset-owned-stop-seed`, `origin=reset-owned-stop-seed`, `handoff=none`, `seed=stop-state`, `halt_guard=skip-live-halt-read`, `legacy=skip-legacy`, `run=run-skip`, `publish=rings-skip`, `next=policy-return`, `followup=fallback-next`, and no expected xHCI diagnostic stage for that skipped attempt. That branch is poll-safe, non-enumerating, and returns immediately to the next probe strategy until stronger ownership evidence exists; the unseeded live-default lane remains the next bounded diagnostic path when the probe ladder can safely select it, and on the Linux-captured high BAR its preflight should report `route=trusted-high-bar-primary`, `halt_guard=skip-live-halt-read`, `ctor=capture-quiesce`, `pre=mailbox-reset-required`, `legacy=skip-legacy`, `next=mailbox-reset-notify`, and `followup=mailbox-reset-then-platform-init`. By default, mailbox ACK without live command proof is stopped before controller entry; the live BCM2711 EXT_CFG replay that can prove BAR/COMMAND remains opt-in diagnostic until it is represented as a bounded HAL-owned PCIe host config transaction, while weaker posted-fallback/soft-continue evidence still falls back to the no-touch `origin=seeded-cold-start` diagnostic route.
- As-built correction: the latest USB traces proved the post-mailbox `USBCMD.HCRST` write, redundant `CONFIG` rewrite, first live `DCBAAP_LOW` store, and then the first live BCM2711 EXT_CFG select are critical Pi 4 high-BAR seL4 edges. `PlatformResetComplete` still keeps the second HCRST and config rewrite skipped, but static Linux capture is no longer accepted as live post-reset ownership. `followup=mailbox-reset-then-platform-init` may publish fresh DCBAA/rings only after a HAL-owned BCM2711 PCIe host config transaction can prove the live VL805 vendor/device/class, BAR decode, COMMAND memory/bus-master bits, and poll-only MSI state without taking the kernel timer IRQ27 halt. If that live proof is missing, local-seat returns before DCBAAP publication instead of touching the wedged store.
- As-built refinement: the seed-backed `ColdStartFromSnapshot` retry lane no longer forces the hostile first live `USBSTS` halt read. If prompt-safe probing falls through to `origin=seeded-cold-start`, preflight should report `halt_guard=skip-live-halt-read`, `legacy=skip-legacy`, and `next=skip-pre-reset`, with the runtime breadcrumbs staying on `0x0204` / `0x0212` / `0x0219` / `0x0217` / `0x0218` / `0x0224` before reset rather than stopping at the first live `0x0213` read.
- As-built refinement: that same trusted seeded cold-start fallback lane now keeps the controller-ready window truly poll-only. Active runtime-owned prompt-safe paths may bind only the bounded pre-controller Pi 4 PCIe/INTx HAL sink set (`180`/`175`); they still remain poll-driven after controller-ready until Cohesix implements the Linux-observed MSI route and an xHCI event-ring clear callback for VL805.
- As-built refinement: the diagnostic pure-F2 strict reply-recovery lane treats the F1/mailbox rearm writes and the immediate Function 2 retry as separate clock phases. On the Pi 4 startup-link path, root-task temporarily drops those diagnostic rearm writes to the startup clock to avoid the observed `sdhci-transfer-finish-data-crc` loop, then restores the promoted `12500000Hz` first-reply lane before the next Function 2 read. `wifi dump-state` reports `reply_probe ... effective_clock=12500000Hz` and `strict_recovery_f2=preserve-latch` only when that diagnostic frontier exists.
- As-built refinement: once that promoted startup-link retry has already failed on the Linux-shaped first Function 2 reply lane, diagnostics now fail fast instead of falling back into a passive startup-link wait tail. The explicit diagnostic path keeps the direct `cyw43-function2-reply-*` blocker, reports `wifi: reply_terminal action=fail-fast retry_clock=12500000Hz`, and skips the old slow `control-plane-passive-startup-link-wait` / `...-timeout` epilogue. Promoted startup-link diagnostics also stay on the Linux `BRCMF_FIRSTREAD` probe rather than walking 32/16/12-byte fallback prefixes first.
- As-built refinement: startup-link `linux-rxfail` reply recovery now only arms the Linux `SMB_NAK` / `linux-rxskip` retransmit path on the active startup-link wait lane. Hintless startup-link first-read probes still stay low-touch, but once Pi 4 is already on the resumed startup-link wait lane it can send the Linux `SMB_NAK`, mark `linux-rxskip` pending, and reuse the known-good retransmit contract instead of immediately collapsing back to a weaker wrapper.
- As-built refinement: when that same startup-link `linux-rxskip` wait is active on the resumed startup-link lane, HAL now yields to the pending wait before applying the promoted-startup-link fail-fast shortcut. That keeps the shared retry loop on the Linux-shaped retransmit contract where recovery is still possible, while the direct `reply_terminal action=fail-fast` path remains in place for the terminal promoted first-read frontier that is not actually waiting on `linux-rxskip`.
- As-built refinement: if a later passive-startup-link or driver-side wrapper still tries to downgrade that same preserved direct first-reply blocker into a weaker `cyw43-control-plane-sideband-*` result, root-task now keeps the direct `cyw43-function2-reply-*` blocker instead. Operator output therefore reports `reply_terminal action=fail-fast` even on the passive-startup-link snapshot when `strict_recovery_f2=preserve-latch` and the blocker class is still `direct-f2-reply`, and `nettest` no longer regresses from the cached direct reply stall back to a weaker sideband wrapper on the same first-reply edge.
- As-built refinement: CYW43 init-failure logging now emits the authoritative control-plane finish snapshot before `debug_dump_state()`. That lets the cached finish snapshot feed the driver’s exact-error preservation path immediately, so `nettest`/`netstats` keep the stronger preserved first-Function-2 blocker instead of falling back to a weaker sideband wrapper just because the earlier live debug snapshot ran first.
- As-built refinement: when post-firmware-ready strict Function 2 repoll fails on an explicit diagnostic startup-link path but the dongle is already `latched-linux-configured-no-iorx`, runtime skips the redundant startup-link channel rearm loop and cuts directly into the bounded first-reply probe. Production boot still fails before this point without strict HT/IOR2 proof.
- As-built refinement: post-firmware-ready strict Function 2 repoll no longer soft-continues into the `post-firmware-ready-function2-sideband` passive-startup-link lane when the failure is already a known first-reply blocker on a diagnostic path. Pi 4 now fails fast at that frontier instead of paying the old slow passive-startup-link tail.
- As-built refinement: when post-firmware-ready strict Function 2 repoll can already prove `latched-linux-configured-no-iorx` but the stricter sideband/core-control reads are unreadable, the exact error now stays in the stronger `cyw43-function2-enable-latched-not-ready-*` family instead of degrading to a generic `cyw43-control-plane-sideband-*` wrapper. That keeps the known-good Linux-shaped Function 2 latch visible to later policy decisions.
- As-built refinement: if a post-write startup-link resume still preserves a known first-reply blocker on an explicit diagnostic lane, root-task no longer parks that path in `passive-startup-link` just because the transport is back on the startup clock. It cuts back to the bounded reply probe immediately, so the Pi 4 no longer burns another passive empty-poll window on a blocker the resume path already proved.
- As-built refinement: if that same post-write startup-link resume returns on the startup clock with a cached `latched-linux-configured-no-iorx` first-reply blocker, root-task now skips the extra `slow-link-channel-rearm` hop and cuts directly to the bounded reply probe. Pi 4 should therefore report `resume-policy mode=direct-reply-probe ... reason=preserved-linux-f2-first-reply-blocker` instead of paying another startup-link Function 2 ready repoll before the next promoted first-reply read.
- As-built refinement: that direct resume cutover now happens before the live startup-link resume path runs when the current CCCR/device state is already visibly `latched-linux-configured-no-iorx` and the preserved blocker is still in the first-reply family. Pi 4 should therefore stop logging a second `startup-link-resume -> slow-link-channel-rearm` block at `400000Hz` on that path and jump straight to the bounded reply probe.
- As-built refinement: once that direct cutover is active, the bounded reply probe no longer waits through the extra zero-frame empty-poll window and `slow-link-channel-rearm` on the `startup-link-resume` lane when the cached blocker is already a preserved Linux-shaped first-reply failure. Pi 4 should therefore jump straight from `direct-speculative-reply-probe` to the promoted speculative read instead of paying one last startup-link resume/rearm tax.
- As-built refinement: when post-firmware-ready strict Function 2 repoll fails but the dongle is still visibly `latched-linux-configured-no-iorx`, runtime no longer allows that visible Linux-configured state to fall back into the generic `strict-repoll-soft-continue` sideband lane. Pi 4 now cuts directly to the bounded first-reply probe from that state, and later init-failure snapshots preserve that stronger first-reply blocker instead of downgrading it to the weaker `cyw43-control-plane-sideband-*` wrapper.
- As-built refinement: when that same post-firmware-ready strict repoll begins with a cached first-reply blocker already preserved on the `first-write-startup-link` lane and the live CCCR/device state is visibly `latched-linux-configured-no-iorx`, runtime now skips the live Function 2 strict-repoll loop entirely and cuts straight to the bounded direct reply probe. Pi 4 should therefore stop burning the extra `reply-strict-recovery` timeout window before the next first-reply read on this path.
- As-built refinement: that direct-cutover startup-link first-reply lane now uses the Linux-sized first control read immediately on the temporary 12.5 MHz reply-probe boost, and once that read itself proves a direct `cyw43-function2-reply-*` blocker HAL fails fast instead of paying a second staged diagnostic-prefix pass, a follow-on `control-plane-reply-prefix-read` probe, or a driver-level first-ioctl startup-link rescue.
- As-built refinement: if that same promoted startup-link first read still lands on a direct `cyw43-function2-reply-*` blocker before the startup-link lane is marked stable, HAL now defers once into the Linux-shaped follow-on first-read diagnostic path instead of treating that unstable direct blocker as immediately terminal. Pi 4 should therefore emit the diagnostic-prefix continuation before the final verdict on that frontier, without falling back into the older passive/rearm tail.
- As-built refinement: if that same cached first-reply blocker already forced the plain `startup-link` direct speculative probe, a zero-length result no longer burns the extra `empty-frame-wait poll=1/8 .. 8/8` window before promotion. Pi 4 now skips straight to the next startup-link rearm/promotion step on that direct-cutover lane, so the final promoted first-reply verdict surfaces without another eight bounded startup-clock polls.
- As-built refinement: if that same preserved direct first-reply blocker reaches the `repeat-no-progress` cutover, root-task no longer routes the final rescue cycle through `startup-link-passive-wait`. Pi 4 now reuses the existing bounded direct-reply cutover immediately, keeping one last bounded reply probe while skipping the extra passive wait / slow-link rearm tail that previously surfaced the same `cyw43-function2-reply-*` blocker more slowly.
- As-built refinement: that same direct-cutover lane now stays off the resumed startup-link `linux-rxskip` / `SMB_NAK` recovery contract entirely. Because the cutover probe deliberately suppresses the Function 1/mailbox hints needed to clear `rxskip`, Pi 4 now keeps the final first-reply probe on plain `startup-link` state and fails directly on the preserved `cyw43-function2-reply-*` blocker instead of burning another `startup-link-resume -> linux-rxskip-pending -> slow-link-channel-rearm` cycle.
- As-built refinement: bounded no-HT firmware-ready now treats the first hostile `sdhci-int-timeout` mailbox read as sufficient evidence to take the existing experimental `assume-ready` fast path. When Function 2 bypass is already active, Pi 4 no longer pays a second full `wait-firmware-ready` recovery ladder before entering `post-firmware-ready-function2-recheck`.
- As-built refinement: on the seeded trusted cold-start retry, runtime no longer emits the mailbox blind-settle breadcrumbs before stop-state replay. Once `origin=seeded-cold-start` is selected, the first explicit ownership breadcrumb should be the seeded `USBCMD` quiesce (`0x0219` and then `0x021a`/`0x021b` or `0x021c`), using the seeded command value directly instead of issuing a fresh live `USBCMD` read on the degraded IRQ27 lane.
- As-built refinement: on that same seeded stop-state handoff lane, runtime still skips any pre-halt polling-interrupter quiesce, but it now stays on the replay branch after the seeded `USBCMD` quiesce. Pi 4 should therefore go straight from `0x0219`/`0x021a`/`0x021b` or `0x021c` into `0x0217`/`0x0218`/`0x0224`, rather than touching the first live `0x0213 stop-revalidation-usbsts-read-begin`.
- As-built refinement: seeded stop-state lanes trust the exported stop-state seed enough to skip the first live pre-reset and halt-revalidation reads. Prompt-safe preflight now reports `halt_guard=skip-live-halt-read` / `next=skip-pre-reset` on those lanes, and Pi 4 should therefore emit `0x0204` before any `0x0217` / `0x0218` / `0x0224` skip-stop breadcrumbs instead of touching the first live `USBSTS.HCH` reread.
- As-built refinement: the seeded trusted Pi 4 cold-start path now keeps the full runtime window poll-only and does not arm bounded trusted sinks after `controller-ready`.
- As-built correction: the `live-runtime-default` lane is the unseeded prompt-safe enumeration path, and the reset-owned stop-state `reset-owned-stop-seed` lane is the first poll-safe diagnostic path when the staged stopped seed is present. IRQ27 remains diagnostic and is not part of the xHCI sink set; on this seL4 build it is the kernel-owned virtual timer PPI and root-task must not request it through IRQControl. HAL owns the reusable seL4 notification wait/poll plus device-clear plus `IRQHandler_Ack` service loop; local-seat may acquire only the generated Pi 4 PCIe host/child INTx GIC sinks (`180`/`175`) before pre-controller ownership writes, while controller-ready USB enumeration remains poll-only until Cohesix has an xHCI event-ring drain/clear callback wired through that HAL loop.
- As-built refinement: the seed-backed high-BAR stop-state contract is not an enumeration path by itself. When local-seat selects `origin=reset-owned-stop-seed` / `handoff=none`, it uses the seed to skip toxic early reads and then records a no-fresh-ownership result unless a stronger ownership step is available. If prompt-safe runtime falls back to `origin=seeded-cold-start`, it also skips root-port sampling and records `enumeration-disabled-bootloader-owned` rather than pretending that no ports were connected.
- As-built refinement: that same seeded stop-state `ColdStartFromSnapshot` lane now skips legacy ownership claim again instead of touching the first live legacy-capability read on the degraded retry path. Prompt-safe fallback therefore reports `legacy=skip-legacy` on `origin=seeded-cold-start`, and a successful seeded skip-live-halt path should emit `0x0212` before `0x0217` / `0x0218` / `0x0224` rather than stopping at `0x0210 legacy-ownership-claim-begin` or the first live `0x0213` reread.
- As-built refinement: on the captured high-BAR stop-state path, runtime now treats the U-Boot stopped seed as reset authority and skips the first U-Boot-shaped HCRST edge that previously preceded the IRQ27 halt report. The latest low-first/high-first DCBAAP traces also prove the stopped seed is not enough authority to publish fresh runtime rings, so that branch now suppresses fresh ring pointers and `RUN` rather than touching DCBAAP. The VL805 mailbox reset notify remains diagnostic and can authorize later resetless stop-state fallback evidence, but posted-fallback/soft-continue evidence no longer blocks the reset-owned stop-seed attempt.
- As-built refinement: on Wi-Fi, once the preserved direct startup-link blocker has already forced the Linux-shaped follow-on first-read diagnostic path, HAL no longer keeps that path in `diagnostic-prefix-only`. A failed follow-on prefix read now remains eligible for one bounded full-block reply read instead of dropping straight back into `fallback-startup-link-empty` at `400000Hz`.
- As-built refinement: if that bounded full-block reply read itself lands on the same Linux `rxfail` / `SMB_NAK` retransmit contract, HAL now returns to `linux-rxskip-wait` instead of immediately classifying the probe as `fallback-startup-link-empty`. Pi 4 should therefore keep the first-Function-2 reply lane on the retransmit continuation Linux uses after `brcmf_sdio_rxfail(..., rtx=true)` rather than fail-fasting on that same timed-out full-block read.
- As-built refinement: once that same Linux retransmit contract is active, Pi 4 no longer keeps the bounded retransmit wait on the toxic hintless pure-Function-2 lane. HAL now re-enables the mailbox / frame-length hint reads while `linux-rxskip` is pending so `HMB_DATA_NAKHANDLED` and the retried full-block reply can surface promptly instead of burning another full `empty-frame-wait poll=1/8 .. 8/8` window at `400000Hz`.
- As-built refinement: that same `linux-rxskip` continuation now disables the broader low-touch pure-Function-2 helper as well, not just the speculative-read hint skip. Once retransmit wait is pending, bounded no-HT reply polling must read the usual Function 1 frame-length/mailbox hints again instead of logging `skip-upfront-f1-frame-len reason=linux-firstread` on the low-clock `startup-link-direct` lane.
- As-built refinement: the one-shot `startup-link-direct` cutover lane no longer qualifies as an active Linux `SMB_NAK` / `linux-rxskip` retransmit-wait lane. If that direct probe has already failed and `linux-rxskip` is still pending, Pi 4 now leaves the direct lane on its bounded no-progress termination path instead of re-entering `linux-rxskip-retransmit-poll` and oscillating forever between `400000Hz` and `12500000Hz`.
- As-built refinement: if that retransmit continuation still burns its bounded empty-poll budget without any Function 2 progress, the `repeat-no-progress-direct-cutover` lane is now truly one-shot. Pi 4 should no longer reset that path back to `speculative-read ... attempt=0` on the plain `startup-link` ladder; after the bounded direct cutover exhausts with no frame progress, HAL now terminates the preserved direct-blocker path instead of looping through the same startup-link sequence again.
- As-built refinement: when that same one-shot `startup-link-direct` lane falls through to the bounded full-block `linux-rxfail` read and successfully arms Linux `SMB_NAK`, Pi 4 now hands that retry back to the normal active `startup-link` retransmit lane instead of terminally classifying the direct full-block probe as `fallback-startup-link-empty`. The direct cutover remains one-shot, but a successful full-block `SMB_NAK` no longer strands the first-reply path on the direct terminal tail.
- As-built refinement: that active `startup-link` Linux retransmit lane now spends the same bounded empty-poll budget as the rest of reply rearm while it is issuing `linux-rxskip-retransmit-poll`. Pi 4 therefore no longer resets the retransmit wait budget back to zero on each speculative poll when hardware never delivers the retried frame; once that shared budget is exhausted, runtime falls back into the existing zero-frame rearm and terminal startup-link timeout path instead of looping forever on the same `linux-rxskip-retransmit-poll` breadcrumb.
- As-built refinement: if a later terminal finish snapshot still performs the low-clock sideband follow-on after that same enable-latched Function 2 read blocker, the cached first-reply snapshot now preserves the stronger `...enable-latched-not-ready-read-*` exact error and `f2-reply-read-*` SDHCI diag instead of degrading the visible failure to the weaker `...enable-latched-not-ready-sideband-*` follow-on artifact.
- Pi 4 trusted preserve-state xHCI is no longer an automatic prompt-safe lane. Preserve-state skips broad post-init cleanup and deliberately preserves bootloader-owned `IMOD`, `ERDP`, `IMAN`, and `USBSTS`; the latest board trace proved the first live `PORTSC` read can halt after `controller-ready`, so preserve-state and seeded bootloader-owned pollsafe probing now skip local-seat root-port sampling and log `xhci root-port sample skipped reason=bootloader-owned-portsc-toxic`.
- As-built correction: when the high-BAR trusted handoff includes a safe halted stop-state snapshot plus bootloader reset authority, Pi 4 prompt-safe runtime tries `origin=reset-owned-stop-seed` first, but the stopped seed now authorizes only poll-safe diagnostics. It skips the first live halt/pre-reset reads and suppresses fresh ownership publishes; after mailbox ACK, the unseeded high-BAR lane retries the HAL-owned BCM2711 EXT_CFG proof only after prior live PCIe link/RC proof and remains stopped unless live BAR/COMMAND evidence is supplied by that HAL PCIe host config path. `origin=seeded-cold-start`, `handoff=cold-start-from-snapshot`, `seed=stop-state`, `publish=rings-skip`, and `post_ready=irq-skip` remain non-enumerating diagnostic evidence for weaker authority states.
- As-built refinement: after the unseeded high-BAR lane receives VL805 mailbox reset confirmation, local-seat treats that mailbox reset as the reset-boundary witness for a poll-only runtime path. The Pi 4 DMA bus alias remains the Linux-captured `0x4_00000000` PCIe window. `platform-reset-complete` enters xHCI constructor only after opt-in live BCM2711 config evidence establishes BAR/COMMAND authority through a HAL-owned PCIe host path and the bounded PCIe sinks have been installed; without that proof, local-seat keeps the platform policy but blocks controller entry instead of touching xHCI ownership registers.
- As-built refinement: when that live `platform-reset-complete` lane is still seeded from the stopped high-BAR state and has live HAL BAR/COMMAND proof, root-task may publish fresh rings and reach controller-ready, but the latest board trace proved the first root-port `PORTSC` read can halt immediately after `RUN`. The stop-seeded platform-reset lane now skips live root-port sampling, logs `reason=platform-reset-stop-seed-portsc-toxic`, drains bounded event-ring port-status-change candidates instead, and records `root-port-sample-deferred` so the UART console survives for the next ownership-safe enumeration attempt. Preserve-state and bootloader-owned stop-seed lanes still skip `PORTSC` sampling.
- As-built refinement: on that same preserve-state diagnostic lane, Pi 4 treats the first post-start `USBSTS` RW1C clear, `ERDP` rewrite, `IMOD` rewrite, `IMAN.IP` clear, and root-port `PORTSC` sample as bootloader-owned touches to skip. The lane can report controller readiness without touching the port register that the latest trace proved fatal.
- As-built correction: Pi 4 trusted xHCI lanes may enumerate by polling after `controller-ready` only after runtime ownership is established and the lane is not still carrying the stop-seed `PORTSC` hazard. The live HAL-proven `platform-reset-complete` lane remains able to publish fresh rings and enter `RUN`, but with a stopped seed present it defers root-port sampling instead of reading `PORTSC`. The reset-owned stop-seed and seeded bootloader-owned pollsafe lanes still skip HCRST, redundant `CONFIG=0x20`, fresh rings, `RUN`, and `PORTSC` sampling because the stopped seed alone is not enough runtime ownership evidence.
- As-built refinement: post-`RUN` scrub and broad post-init quiesce are disabled because post-ready interrupter/status touches remain fragile ownership edges after `controller-ready`. Enumeration proceeds from the poll-only state with no post-ready `IMOD`, `IMAN`, or pre-consumption `USBSTS.EINT` writes.
- As-built refinement: trusted Pi 4 `ColdStartFromSnapshot` handoffs, both unseeded and seeded, now skip the post-`controller-ready` `IMOD` / `IMAN` zeroing edge entirely. Enumeration is poll-driven from the freshly published runtime rings, removing the last known `0x0267 post-ready-imod-write-wedged` edge before local-seat can enumerate ports.
- As-built refinement: Wi-Fi first-reply probing no longer borrows the promoted bulk clock while reply rearm mode is still `startup-link` or `startup-link-resume`. The startup-safe first-reply lane stays pinned to the true startup profile (`400000 Hz`, `4bit`) until a real Function 2 reply completes, and only a later proven reply path may emit `action=reply-probe-bulk-clock`.
- As-built refinement: `wait-firmware-ready` no longer fabricates mailbox readiness from fragile `sdhci-int-timeout` or `cmd52` failures. Pi 4 now advances only after a real `tohostmailboxdata` read succeeds, which keeps firmware bring-up aligned with the known-good Linux/WHD mailbox contract instead of synthesizing `DEVREADY|FWREADY|version`.
- As-built refinement: bounded no-HT Wi-Fi no longer clears/re-enables Function 2 after an `IOEX.F2=1` / `IORX.F2=0` latch during startup-link rearm. The strict lane still requires real `IORX.F2`; the Pi 4 post-release forced ALP/KSO tuple may be inspected only by an explicit bounded diagnostic probe.
- As-built refinement: the former no-HT firmware-channel Function 2 clear/re-enable edge is disabled. Root-task still preserves the Linux ordering (`mailbox -> Function 2 -> hostintmask -> watermark -> devctl`) on the strict lane, and any diagnostic Pi 4 latch path preserves `IOEX.F2` rather than replaying a clear/re-enable cycle before its short probe budget.
- As-built refinement: the first bounded startup-link reply attempt now keeps the Linux-style F1 hint + `linux-firstread` path until sideband reads are proven unstable or the startup link is already stabilized. Pi 4 no longer suppresses those hints and force a hintless pure-`F2` first read on the very first `startup-link` attempt.
- As-built refinement: Pi 4 Wi-Fi now matches the Linux/WHD backplane-prep clock contract more closely before firmware boot. Root-task asserts `FORCE_HW_CLKREQ_OFF | ALP_AVAIL_REQ`, waits for ALP, then holds `FORCE_HW_CLKREQ_OFF | FORCE_ALP` (`0x21`) with a short bounded settle before the ARM core reset / HT wait path instead of dropping CHIPCLKCSR back to `0` or holding only `0x20`. The Linux `SDIOPULLUP` (`0x1000F`) clear remains disabled in the seL4 path until Function 1 sideband access survives the post-reset `CHIPCLKCSR` HT request; the Pi 4 capture does not show that clear before the HT polling pattern, and the latest Cohesix trace showed an end-bit error on the following `CHIPCLKCSR` CMD52 when the clear was attempted too early. The optional pre-reset HT assist is disabled on this path so the first ARMCR4 control access does not replace the known-good ALP backplane contract with a stronger HT-only request.
- As-built correction: before the ARMCR4/SOCRAM reset-control sequence, Pi 4 Wi-Fi now programs CCCR high-speed timing and raises the SDIO request to the Linux high-speed control-plane target. Earlier builds restored the high-speed lane only after firmware/NVRAM upload, leaving the first ARMCR4 clear-reset proof at the fragile `399361 Hz` startup divider.
- As-built refinement: after firmware/NVRAM staging, Pi 4 Wi-Fi restores the Linux high-speed activation lane before the reset-vector write and ARMCR4 release, including the case where the bounded verify fallback ended on the conservative 1-bit retry lane. That restore emits `firmware stage=firmware-release-width action=restore-4bit ... reason=linux-pre-armcr4-ht-proof`, keeps the 4-bit lane through the required `wait-ht-clock` poll, and skips the extra post-release ARMCR4 readback because the latest hardware trace showed that read only timed out and forced host recovery before HT. It now emits a bounded `post-release-proof` tuple that points back to the existing `core-reset` verification log and captures the cached clock/sideband state at the ARMCR4 release boundary. The path no longer demotes the HT transition back to the 400 kHz startup lane after a successful upload.
- As-built refinement: the `post-release-proof` tuple now records the firmware upload range, normalized NVRAM range, NVRAM tail magic, reset-vector address/value, CPUHALT release value, upload/current/preferred SDIO clocks, and cached `CHIPCLKCSR` / `WAKEUPCTRL` / `SLEEPCSR` / `CARDCAP` labels. It is diagnostic-only and uses existing cached state, so it does not reintroduce the fragile post-release ARMCR4 or Function 1 readback that previously blocked HT proof.
- As-built correction: the normal Pi 4 CYW43455 firmware and control-plane target now matches the Linux capture by programming CCCR `SPEED.EHS`, setting SDHCI `HOST_CONTROL.HISPD`, and requesting a 50 MHz high-speed 4-bit SDIO lane (Linux final actual clock was `41666667 Hz` from the `fe300000.mmcnr` parent divider). Firmware image upload uses the same Linux-shaped high-speed Function 1 block-transfer lane first, with 32 KiB windowed writes, then retries at 25 MHz and 12.5 MHz before falling back to 1.5625 MHz byte-mode. Successful HT/F2 paths no longer lower `wait-firmware-ready` or pre-HT control-plane probes back to 400 kHz just to poll the mailbox.
- As-built refinement: Pi 4 Wi-Fi keeps the Linux/OpenBSD probe-attach order and now treats pre-firmware KSO as wake evidence only, not clock proof. Firmware load mirrors upstream `brcmf_sdio_kso_init()` by setting `SLEEPCSR.KSO` without requiring `DEVON`, then performs `CARDCTRL.WLANRESET`, best-effort ChipCommon `PMUCONTROL.RES_RELOAD` with live readback skipped, keeps F2 disabled before firmware upload, and clears `CHIPCLKCSR=0` before the ARMCR4/SOCRAM reset/upload sequence. After ARMCR4 release, the required HT path preserves the live sideband state and requests `HT_AVAIL_REQ` while still requiring real `HT_AVAIL` before any Function 2 readiness check; SR sideband remains refreshed again after Function 2 readiness.
- As-built correction: after firmware/NVRAM staging and before ARMCR4 release, Pi 4 Wi-Fi now mirrors `brcmfmac` activation by clearing SDIO-core `intstatus` with `0xffffffff` and writing the little-endian firmware reset vector from bytes `0..4` to backplane address `0x00000000`. That closes the gap where firmware bytes were present at `0x00198000` but the ARMCR4 release had no reset-vector handoff.
- As-built correction: ARMCR4 passive/active setup now follows the Linux `brcmfmac` resetcore shape. The passive stage leaves ARMCR4 clocked and CPU-halted while firmware/NVRAM are staged, and the post-reset-vector release re-enters reset with CPUHALT asserted before clearing CPUHALT and releasing ARMCR4. The required post-release clock wait treats cached `SLEEPCSR=0x03` DEVON as diagnostic only, preserves only a freshly read live device-on `SLEEPCSR=0x03` state, writes KSO-only (`0x01`) when a production SLEEPCSR request is needed, freshly writes `CHIPCLKCSR=0` for the Linux firmware-callback clock-off transition even when the ARMCR4 release fence already ran, then writes `CHIPCLKCSR.HT_AVAIL_REQ`, and watches for a real `HT_AVAIL` bit. A stable `0x50` timeout shape is a blocker, not authority to assert `FORCE_HT` or attempt mailbox/control traffic without the strict ready bit.
- As-built refinement: the seeded Pi 4 `origin=seeded-cold-start` xHCI lane no longer attempts a fresh Cohesix-owned event-ring publication when the trusted seed contains only halted stop-state and no runtime-ring pointers. The latest trace advanced past the earlier post-ready `ERSTSZ` edge and then halted at `0x02bb tag=erdp-target-low`, proving that the first live ERDP low-dword store is toxic on this weak lane too. Runtime now emits the bootloader-owned poll-safe skip breadcrumbs instead of writing ERDP/ERSTSZ/ERSTBA on that path.
- As-built correction: that seeded stop-state fallback is no longer treated as a half-preserved runtime-ring handoff. If runtime reaches it, the lane skips live HCRST/CONFIG because the bootloader stop-state seed is the reset authority, leaves `IMAN` untouched before runtime owns any fresh event ring, skips the pre-`RUN` `USBSTS` clear, skips the live `USBCMD.RUN` write with `0x02e8 fw-handoff-trusted-usbcmd-run-skip`, and now also suppresses fresh `DCBAAP`, command-ring, event-ring, and `DNCTRL` ownership stores. Any command submission on that bootloader-owned poll-safe lane returns `NotSupported` rather than ringing a doorbell against rings the controller has not adopted.
- As-built refinement: on the strict Pi 4 startup-link Wi-Fi lane, the first fragile firmware-channel mailbox write and the immediate `F2-ready` dwell now stay at `400000 Hz`, and only restore the promoted transport before `hostintmask` / watermark / `devctl`. This keeps the Linux/WHD `mailbox -> F2 -> hostintmask` order intact while avoiding both the `setup-firmware-channel-mailbox-version` and promoted `sdio-function2-ready` edges at `12500000 Hz`.
- As-built correction: Pi 4 Wi-Fi now treats the Linux pre-firmware `brcmf_sdio_htclk CLKCTL: turned ON` edge as ALP-only because Linux sets `bus->alp_only = true` before firmware download. Cohesix requests `ALP_AVAIL_REQ`, accepts `ALP_AVAIL` for firmware/NVRAM upload, and reserves the real `HT_AVAIL_REQ`/`HT_AVAIL` proof for the post-release Function 1 sideband and clock-request transition before firmware-channel Function 2 traffic.
- As-built refinement: bounded no-HT firmware-channel Function 2 is disabled for production boot. All normal callers keep the strict Function 2 ready contract and require real `CHIPCLKCSR.HT_AVAIL` plus `IORX.F2` before firmware or control-plane traffic can move.
- As-built correction: Pi 4 Wi-Fi no longer cuts production boot over to bounded no-HT transport after a forced ALP/KSO tuple. The latest hardware trace with `CHIPCLKCSR=0x12/0x52`, `IOEX=0x06`, and `IORX=0x02` proves progress to a Function 2 latch but not readiness. Root-task now keeps the Linux/brcmfmac clock contract split by phase: F2 stays disabled before firmware upload, firmware upload requires only the Linux pre-download ALP edge, the production post-release transition preserves live sideband, writes the SD-only `CHIPCLKCSR=0` fence, then writes only `CHIPCLKCSR.HT_AVAIL_REQ`, and the strict lane still requires CCCR `IOR2`; any sideband primer or short direct F2 probe is explicit diagnostics only.
- As-built correction: Pi 4 firmware staging follows the Linux `brcmf_sdio_download_firmware()` order by writing the firmware image at `0x00198000`, writing the NVRAM/tail near the top of RAM, then releasing ARMCR4 only after the current Cohesix gate is satisfied. On ALP-only uploads that gate attempts bounded readback of the fully staged image/NVRAM/tail before ARMCR4 release; proven byte mismatches remain fatal, while transport-unavailable readback after all bounded retry profiles is advisory and allows ARMCR4 release with `verified=no`. Cohesix does not read the image before NVRAM; it verifies after complete staging so the bounded retry profiles can classify the real transport edge. Firmware-image verification starts with the Linux-shaped 2048-byte Function 1 block-read shape instead of the generic 256-byte byte-mode backplane read.
- As-built correction: Pi 4 Wi-Fi can bind the SDIO host IRQ (`GIC hwirq 158`, Linux `mmc1/mmcnr@7e300000`) through HAL, but default CYW43 bring-up remains poll-only until the Function 2 and device-source clear path are proven. Root-task keeps the Linux-observed firmware-channel order for mailbox version, `HOSTINTMASK`, watermark, `DEVCTL(F2WM_ENAB)`, and `MESBUSYCTRL`, uses the captured CYW43 SDIO core window at `0x18004000` (`intstatus=0x18004020`, `hostintmask=0x18004024`, `FUNCTIONINTMASK=0x18004034`, `tohostmailbox=0x1800404c`), and defers `FUNCTIONINTMASK`, `CCCR.IENx`, and SDHCI `CARD_INT` signal arming by default. An interrupt-driven experiment must first service the device source by reading/masking SDIO-core `intstatus` against `HOSTINTMASK` and clearing SDHCI `CARD_INT`, then call `IRQHandler_Ack`.
- As-built correction: Pi 4 Wi-Fi WL_ON now follows reset-gated pwrseq semantics. `set_power(On)` leaves the line low while reset is asserted, `set_reset(Deasserted)` drives the real low-to-high WL_ON edge, and recovery asserts reset before power cycling so the module sees the same reset edge Linux relies on before SDIO enumeration. KSO wake still records `SLEEPCSR.KSO` as the first wake acknowledgement; the production post-firmware transition now preserves the live post-release sideband, replays the Linux SD-only chip-clock fence, and requires real HT proof before Function 2. The clear-then-KSO DEVON sequence remains diagnostic-only, while missing DEVON remains sideband evidence rather than permission to enter Function 2.
- As-built refinement: when Pi 4 has a trusted halted stop-state snapshot (`seed_flags=0x03`, `ring_seed=false`), local-seat records that seed for the reset-owned stop-state primary path, the bounded seeded cold-start fallback, and diagnostic preserve-state. Prompt-safe probing should report `route=trusted-high-bar-stop-seed-primary`, `policy=reset-owned-stop-seed`, `origin=reset-owned-stop-seed`, `handoff=none`, `seed=stop-state`, `pre=seeded-no-hcrst`, `run=run-skip`, `publish=rings-skip`, and `post_ready=irq-skip` on that poll-safe attempt. `route=trusted-high-bar-seeded-retry` / `origin=seeded-cold-start` remains the no-touch fallback for weaker authority states.
- As-built refinement: if preserve-state is selected, local-seat keeps that lane poll-only after `controller-ready`, reports `post_ready=irq-skip`, skips both the narrow `0x0315` post-init quiesce and the broader `0x0316` post-run scrub path, suppresses the `0x0330`/`0x0331`/`0x0332` `IMAN.IP` pending-clear path, and avoids root-port sampling. The trusted cold-start and runtime-owned lanes also avoid direct post-ready `IMOD` / `IMAN` zeroing.
- As-built trace refinement: `wifi dump-state` now emits raw CCCR/F1 register evidence, SDHCI shadow registers, backplane window bytes, and the strongest preserved failure snapshot after the SDHCI contract lines. A hard required HT wait now also caches `cyw43-ht-clock-timeout-before-function2`, so a later shell dump no longer collapses that failure into an off/reset fresh snapshot.
- As-built trace refinement: `usb probe-kbd` now emits the expected IRQ contract in preflight, and `usb status` / `usb dump-state` / `usb probe-kbd` mirror the latest route-bound xHCI diagnostic tuple as `usb: diag_contract ...` plus `usb: diag_values ...`. Captured high-BAR capability snapshots can pin a bounded local mapping and select the default Cohesix-owned cold-start lane as layout evidence, but they remain `trusted=0` for bootloader state and never authorize preserved firmware rings; raw delivery is limited to the HAL-bound Pi 4 pre-controller PCIe/INTx sink set described above. This makes the shell transcript sufficient to identify whether the active route died on policy, pre-`RUN` publish, `RUN`, post-ready cleanup, or later enumeration.
- As-built trace refinement: boot-time diagnostics now include a single xHCI handoff contract line as local-seat attaches, including raw/normalized BAR hint, PCI command presence, U-Boot handoff/reset-authority tokens, capability/stop-state snapshot presence, and the current poll-only IRQ policy. Wi-Fi SDIO init also emits an IRQ contract line with `GIC hwirq 158`, level trigger, HAL binding badge, SDHCI `INT_STATUS` / `INT_ENABLE` / `SIGNAL_ENABLE`, and the device-clear-before-`IRQHandler_Ack` contract. The firmware-ready wait logs a bounded first/final idle notification sample plus timeout diagnostics, so a boot log can now distinguish no device interrupt, missing SDHCI signal, and seL4 notification delivery with uncleared device state.
- As-built correction: the trusted Pi 4 high-BAR xHCI route no longer applies the Broadcom generic-wrapper AXI attribute quirk. The VL805 PCI/U-Boot path treats BAR0 as a normal xHCI PCI controller, so `origin=uboot-fresh-init` now reports `axi_setup=false`, skips the live `0x0c08/0x0c0c` AXI reads that halted the latest board, skips xHCI legacy-ownership walking for `ColdStartFromSnapshot`, and masks `DBOFF`/`RTSOFF` reserved bits like U-Boot before sizing the MMIO window.
- As-built correction: the current Pi 4 Wi-Fi clock gate treats `SLEEPCSR.KSO` as wake acknowledgement only. The production post-release HT request primes `WAKEUPCTRL.HTWAIT` and CCCR `CARDCAP=CMD14_SUPPORT|CMD14_EXT`, preserves live `SLEEPCSR.KSO|DEVON` without the clear/set ladder when already ready for Function 2, but no longer makes live DEVON a terminal pre-HT sideband gate. A KSO-only live value may issue the fresh `CHIPCLKCSR=0` fence and `CHIPCLKCSR.HT_AVAIL_REQ`; production then polls for a real `HT_AVAIL` bit through the full bounded Linux-style active wait and treats stable `0x50` (`HT_AVAIL_REQ|ALP_AVAIL`) as a terminal timeout only if the chip still does not progress by the end of that window. Production does not assert `FORCE_HT` before HT proof and does not run a second HT clock ladder; manual `wifi diag` / `probe-ht` may still inspect forced tuples separately.
- As-built correction: the VL805 runtime reset notify now tries both accepted VideoCore bus aliases with the full mailbox receive budget before using the bounded posted fallback. If neither alias returns a reply token or a late completed response buffer, root-task logs `mailbox timeout-buffer tag=notify-xhci-reset ...` for each alias and keeps the non-freezing bootloader-owned poll-safe fallback instead of authorizing a live reset edge.
- As-built correction: the high-BAR path treats live PCI config evidence as necessary authority for the `platform-reset-complete` policy, and that evidence is now gathered through the HAL-owned BCM2711 EXT_CFG proof path after the bounded PCIe sinks are installed. Linux resets VL805 before xHCI startup; Cohesix keeps generic VL805 ECAM reads/writes disabled and uses only the BCM2711 host EXT_CFG registers. The proof first powers the Pi firmware USB HCD domain through the same mailbox module ID U-Boot uses (`3`), validates PCIe link state and root-complex mode, performs one bounded HAL-owned root-complex reset/window init if the live status gate is initially missing, drains posted reset/window writes with same-block readbacks, waits through a conservative spin-backed U-Boot/PCIe CEM 100 ms post-PERST target plus bounded 100 ms link-poll target, and if status remains inconclusive allows only exact live EXT_CFG promotion for the known VL805 vendor/device/class/BAR tuple before COMMAND and poll-only MSI proof. After the mailbox reset boundary, local-seat now runs a distinct HAL-owned EXT_CFG proof retry even when the stale pre-reset status did not prove link/RC readiness, but if that post-mailbox status lacks the PCIe data-link/PHY-link bits, HAL logs `reason=link-not-active exact=deferred stage=post-mailbox-reset` and returns before the first EXT_CFG_DATA read. The retry remains non-authoritative unless the live tuple and COMMAND/BAR proof succeed. A mailbox-completed high-BAR reset without live replay proof stays on the platform policy and returns before controller entry; with HAL-owned proof, the `platform-reset-complete` lane treats the mailbox reset as the reset boundary, skips HCRST/CONFIG, publishes fresh DCBAA/rings, and proceeds to the poll-driven `RUN`/controller-ready path.
- As-built correction: the default live-proven `platform-reset-complete` lane no longer repeats the post-mailbox HCRST or CONFIG rewrite edge. Local-seat explicitly acknowledges the already-bound PCIe host/child INTx sinks (`180`/`175`) and then lets `usb-oxide` publish local DCBAA/rings under the existing diagnostic breadcrumb ladder.
- Current M26b correction: the latest board trace supersedes the older no-HT wording above. Wi-Fi production boot keeps Function 2 disabled until real post-release HT proof appears, but firmware upload no longer falls immediately to slow byte-mode just because the pre-download phase is ALP-only; it follows Linux's high-speed 4-bit Function 1 block-transfer shape first and keeps byte-mode as the last fallback. The 32 KiB Function 1 firmware write encodes the CMD53 count field as zero for 512 blocks, so the first upload should report `addr=0x08000`, `blkcnt=512`, and `count=0` rather than corrupting the flagged Function 1 address to `0x08001`; if that bulk write returns `sdhci-int-timeout`, the firmware-upload path now treats it as a recoverable transfer failure and enters the existing clock step-down / byte-mode retry ladder instead of aborting before NVRAM and ARMCR4 release. On an ALP-only upload, staged firmware image/NVRAM/tail readback is attempted before ARMCR4 release; the image proof starts with 2048-byte Function 1 block reads like Linux, and read-unavailable retries now continue to ARMCR4 release with `verified=no` rather than masking the next real gate behind a pre-release policy stop. A byte mismatch remains terminal. A later `cyw43-function2-disabled` snapshot with `CHIPCLKCSR=0x50` and `IOEX/IORDY=0x02` is reported as `wait-ht-clock`, not `function2-ready`, because it proves HT is still missing and Function 2 remains disabled. USB still treats raw Linux VL805 capture as layout evidence only. After mailbox ACK, local-seat retries the HAL-owned BCM2711 EXT_CFG proof on the high-BAR candidate even if the pre-reset status did not prove link/RC readiness, and only promotes `platform-reset-complete` when live COMMAND/BAR evidence is present. The BCM2711 PCIe host pages are mapped in ascending physical order (`0xfd504000`, `0xfd508000`, then `0xfd509000`) before root init consumes the SW_INIT/EXT_CFG_INDEX page, because seL4 device-untyped retyping is monotonic and a later-page allocation can make an earlier exact page unmappable. Each live EXT_CFG byte/word/dword access now reselects VL805 `01:00.0` through `EXT_CFG_INDEX` before touching `EXT_CFG_DATA`; after mailbox ACK, HAL now refuses that first EXT_CFG_DATA read when the live status only proves port presence and lacks data-link/PHY-link bits. The selector echo value `0x00100000` is explicit non-proof rather than a promotable vendor/device tuple. Fresh publication remains blocked if that proof is absent; IRQ27 is the seL4 virtual-timer PPI and is not used as a USB interrupt or as proof of the VL805 edge.
- Current M26b USB correction: after the post-mailbox VL805 reset boundary, a live `status=0x000000b0` root/link proof plus exact VL805 `1106:3483` / class `0x0c0330` tuple may still expose an unassigned endpoint BAR as `BAR0=0x00000004`, `BAR1=0x00000000`. That tuple is a 64-bit memory BAR with only type bits set, not a usable MMIO base. HAL now performs the missing PCI resource-assignment step for that exact case only, writing the Pi 4 outbound-window BAR value `0xc0000004/0x00000000`, reading it back, and logging `vl805 bcm2711-pcie bar assign ... reason=unassigned-64bit-memory-bar` before COMMAND/MSI proof. Any other bad BAR remains a blocker before xHCI registers or rings are touched.
- Current M26b Wi-Fi correction: firmware image verification still starts with the Linux-shaped 2048-byte Function 1 block read, but the bounded retry profiles now actually change the transfer shape. When `firmware_verify_profile ... byte_mode=y` is selected, root-task verifies the staged image/NVRAM/tail through 512-byte CMD53 byte-mode chunks at the 400 kHz 4-bit and 1-bit fallback lanes instead of replaying the same failing 2048-byte block transfer. Logs should show `firmware_verify_profile ... chunk=512 byte_mode=y` followed by Function 1 byte-mode `sdio xfer chunk` lines if the first proof read fails.
- Current M26b USB correction: after live HAL EXT_CFG proof assigns BAR0 and COMMAND, the `platform-reset-complete` stop-seed lane publishes Cohesix-owned DCBAA/rings and performs the U-Boot-style `USBCMD.RUN` store, but treats immediate post-RUN `USBCMD`/`USBSTS` live reads and the first root-port `PORTSC` read as hazardous on that stop-seed path. The RUN store completion breadcrumb is now distinct (`0x02e5 usbcmd-run-write-done`); `0x0110 controller-init-complete` marks successful constructor/init return; and `0x02da` returns to its preserve-state ERSTSZ-skip meaning. A stop-seed platform-reset lane now does a bounded blind settle after the RUN store, avoids an implicit Drop-time live stop on fallback/no-keyboard return, logs `root-port sample skipped reason=platform-reset-stop-seed-portsc-toxic`, drains event-ring `PORT_STATUS_CHANGE` TRBs into an `event_candidate_mask`, and keeps `connected_mask=0` so event evidence is not mislabeled as cached `PORTSC`. When event candidates exist, local-seat performs a tiny command-ring-only proof (`enable_slot` followed by best-effort `disable_slot`) and reports `command_probe=enable-slot-ok` / `verdict=command-ring-ready` on success. When no event candidates exist, it submits a command-ring No Op TRB through a poll-only wait and reports `command_probe=no-op-ok` / `verdict=command-ring-ready-no-port-event` on success. It still records `root-port-sample-deferred` instead of touching `PORTSC` or entering `UsbDevice::new`.
- Current M26b Wi-Fi correction: probe-attach KSO initialization now follows upstream `brcmf_sdio_kso_init()` rather than runtime `brcmf_sdio_kso_control()`. Root-task sets/observes `SLEEPCSR.KSO` at `stage=linux-probe-attach-kso` before CARDCTRL, PMU reload, or core reset work; `DEVON` is not a pre-upload contract on this path.
- As-built correction: if U-Boot exports the trusted high-BAR post-stop contract with halted stop-state evidence, Cohesix records that as authority only for the poll-safe `origin=reset-owned-stop-seed` diagnostic path. That seeded path is now the first attempt so Cohesix avoids the live `USBCMD`/`USBSTS` reads that the latest board trace proved toxic, but it no longer publishes rings or writes `RUN`.
- As-built correction: prompt-safe high-BAR probing now treats any trusted halted stop-state snapshot as sufficient to try the poll-safe `origin=reset-owned-stop-seed` lane before the non-enumerating `origin=seeded-cold-start` diagnostic lane. That keeps posted/soft fallback captures from collapsing directly to the weaker bootloader-owned route while still requiring stronger ownership evidence before any Cohesix-owned ring-publish path can enumerate.
- Boot must fail before ticket registration if:
- attestation is required/enabled and policy cannot be satisfied.
- `hw.local_seat.required=true` and local-seat initialization cannot be satisfied.
- If `hw.local_seat.required=false`, runtime must degrade to serial-only diagnostics with explicit `[local-seat] degraded ...` boot lines.

## Bootloader/HAL ownership boundary
- Bootloader-owned (pre-handoff): Pi firmware + U-Boot setup, media loading, pre-boot env/network commands.
- Cohesix-owned (post-seL4): HAL-backed runtime device access only (UART, local-seat paths, attestation plumbing, network handoff points).
- Root-task runtime code must not call bootloader/firmware services directly after seL4 entry.
