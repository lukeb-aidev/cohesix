<!-- Author: Lukas Bower -->
<!-- Purpose: Track Milestone 26c documentation and script drift findings. -->
<!-- Copyright 2026 Lukas Bower -->

# M26C Doc Drift Ledger

Status: `CLOSED / NONBLOCKING-DEFERRALS-RECORDED`

| ID | Surface | Drift | State | Evidence | Required Closure |
| --- | --- | --- | --- | --- | --- |
| M26C-DOC-001 | `docs/TEST_PLAN.md` and runner scripts | Test Plan lacked target-qualified runner semantics required by 26c. | Closed | `scripts/ci/check_test_plan.sh` PASS; QEMU/Pi Stage 01 smoke PASS; QEMU Stage 01-05 PASS in `out/test-plan/m26c-qemu`; QEMU Stage 05 due diligence PASS at `out/audit/gate/20260628T015332Z`; Pi Stage 01-05 PASS in `out/test-plan/m26c-pi4-live`; Pi Stage 05 due diligence PASS at `out/audit/gate/20260629T061204Z` | Keep QEMU and Pi proof lanes separate in future audits. |
| M26C-DOC-002 | Canonical Mermaid diagrams | Some canonical diagrams used raw HTML line breaks or duplicated prose better owned by focused documents. | Closed for active docs | The README-linked suite was reduced from 18 to nine owned diagrams; the final tracked inventory contains 75 blocks, all of which rendered with Mermaid CLI 11.16.0. `scripts/ci/check_mermaid_github.sh` passes active surfaces. | Keep the checker and full render audit in the documentation gate. |
| M26C-DOC-003 | Release Mermaid snapshots | Release-derived copies still contain raw HTML labels. | Deferred-nonblocking | 32 warnings from Mermaid checker; Stage 05 `scripts/ci/check_test_plan.sh` PASS | Fix only through release-cut flow; do not hand-edit release snapshots for style. |
| M26C-DOC-004 | Pi runtime/DMA proof wording | Docs distinguish compiler-owned DMA profile truth, stage-only target-build provenance, normalizer proof classification, live proof bundles, direct TCP proof, and Pi Stage 05 proof-artifact enforcement. | Closed | `scripts/pi4-image-build.sh`; `scripts/pi4_trace_normalize.py`; final GENET gate proof `out/test-plan/m26c-pi4-live/pi4-runtime-dma-proof-genet-latest.env`; direct TCP proof `out/test-plan/m26c-pi4-live/cohsh-tcp-proof-genet-latest.txt`; `scripts/ci/test_plan_run.sh`; Stage 05 due diligence `out/audit/gate/20260629T061204Z` | Keep earlier WiFi and older wired proof artifacts as comparison only; final Pi closure uses the GENET proof chain. |
| M26C-DOC-005 | Worker public docs vs implementation | Generated worker role state, no_std worker loops, endpoint badge validation, notification badge classes, and non-MCS scheduling evidence now exist for QEMU closure. | Closed-QEMU / full future cap-bundle not claimed | `cargo test -p coh-rtc worker_runtime`; `cargo test -p worker-heart -p worker-gpu -p worker-lora`; `cargo test -p root-task --test worker_authority`; `cargo check -p worker-heart -p worker-gpu -p worker-lora --target aarch64-unknown-none` | Keep future full cap-bundle and Pi runtime/DMA proof out of QEMU worker claims. |
| M26C-DOC-006 | AI-fingerprint wording | Generic comments and marketing phrasing remain in some authored surfaces; QEMU-touched worker crate wording was cleaned while generated/release/vendored surfaces were left alone. | Closed for QEMU-touched files / broader cleanup deferred | `apps/worker-heart/src/lib.rs`; `apps/worker-gpu/src/lib.rs`; `apps/worker-lora/src/lib.rs`; `out/audit/m26c_ai_fingerprint_rg.txt` | Address broader host/doc cleanup only through accepted Phase 4 waves with characterization evidence. |
| M26C-DOC-007 | Pi proof lifecycle normalization | Fresh Pi logs can resolve shell-first bootstrap deferrals, current DMA blockers, current resource blockers after later owner-state/DMA proof, post-first-byte USB blockers, glued serial trace segments, and zero-valued budget-overrun pressure fields after prompt-side replay. | Closed | Fresh log `/Users/lukasbower/pi4-serial-20260629-135454.log`; `.venv/bin/python -m pytest tests/test_pi4_trace_normalize.py tests/test_pi4_gate_proof.py`; final GENET runtime/DMA proof `out/test-plan/m26c-pi4-live/pi4-runtime-dma-proof-genet-latest.env`; Stage 05 `cargo clippy --workspace --all-targets -- -D warnings` PASS at `out/audit/gate/20260629T061204Z` | USB old-good replay remains separate from M26c closure and must not replace staged Pi proof. |
| M26C-DOC-008 | Prompt-side USB owner-state proof re-emission | One prompt-side USB first-report path registered owner-state without immediately requesting the canonical boot-contract proof re-sample. | Closed | `apps/root-task/src/local_seat.rs`; rebuilt/reflashed image; final Pi Stage 05 workspace tests at `out/audit/gate/20260629T061204Z/workspace-tests.log` | Classified as proof-emission repair, not USB behavior expansion. |
| M26C-DOC-009 | Latest GENET proof separation | User-reported live GENET boot must not replace newest captured evidence unless the serial/pcap pair and live probes prove it. | Closed | Final GENET serial `/Users/lukasbower/pi4-serial-20260629-135454.log`; paired pcap `/Users/lukasbower/tcpdump-usb-eth-20260629-135504.pcap`; direct probes and `cohsh` proof at `192.168.10.50`; Stage 03/04/05 PASS markers in `out/test-plan/m26c-pi4-live` | Final closure uses one coherent GENET boot; older WiFi and earlier wired evidence remain comparison only. |
| M26C-DOC-010 | README-linked canonical suite | Contracts were repeated across files; several current-state, transport, authority, provider, mock, simulation, and proof claims were stale or ambiguous. | Closed | `m26c-readme-linked-doc-suite-remediation`; a 17-document suite plus `README.md`; 58.7% fewer narrative lines; 266 resolved local links; generated-doc tests, `scripts/check-generated.sh`, `cargo check --workspace`, Test Plan checks, and 75-of-75 Mermaid rendering PASS. | Preserve the document ownership and source maps; update generated mirrors through `coh-rtc`; keep current-image, historical, simulated, and planned evidence distinct. |
| M26C-DOC-011 | Markdown and Mermaid inventories | The checked-in Markdown inventory covered 186 of 203 tracked files, and all Mermaid compatibility statuses remained `pending-check` after validation. | Closed | Regenerated `M26C_MARKDOWN_INVENTORY.csv` and `.md` with 203 entries; regenerated 75 Mermaid rows and finalized 43 as `pass` plus 32 as `warning-render-pass`; 75 SVGs rendered. | Regenerate both inventories from `git ls-files '*.md'` whenever tracked Markdown changes, then record compatibility results after the checker and render pass. |
| M26C-DOC-012 | Hardware proof-layer diagram | The diagram merged flash with readback and omitted saved-policy proof even though the owning prose defined nine independent states. | Closed | `docs/HARDWARE_BRINGUP.md` now shows all nine states; final Mermaid CLI render PASS. | Do not collapse proof states in diagrams or summaries. |
| M26C-DOC-013 | REST OpenAPI and narrative mirror | `lines` was assigned to `CAT` instead of `TAIL`, and `HOST_API.md` duplicated a stale schema. | Closed | `resources/openapi/hive-gateway.yaml` matches the current handlers; `docs/HOST_API.md` is now a concise link-backed reference; `cargo test -p hive-gateway` PASS. | Update OpenAPI, handler tests, and narrative guidance together; do not duplicate the schema in prose. |
| M26C-DOC-014 | Reopened 26b status | The plan still described poll/yield as current after the generated IRQ/DPC topology and isolated-runtime service had landed. | Closed | `docs/BUILD_PLAN.md` and `docs/DRIVERS.md` now agree: implementation landed, repeated current-image Wi-Fi functional proof pending. | Keep implementation state and hardware acceptance state separate. |
| M26C-DOC-015 | README project introduction | The suite rewrite removed the Plan 9 lineage, assumed readers already understood seL4, 9P, capabilities, and NineDoor, and led with project status rather than purpose and value. | Closed | `README.md` now introduces the problem and differentiators, defines each foundation in plain language, explains why Cohesix combines them, distinguishes kernel proof from whole-system assurance, and preserves the host Secure9P versus target-console boundary. | Keep the landing page accessible to readers without operating-system or Plan 9 experience while preserving as-built and evidence boundaries. |

## Notes

Generated snippets, release snapshots, vendored Markdown, seL4 mirrors, and
append-only audit evidence are not style-cleanup surfaces. They require their
source generator, release-cut, vendor import, accepted reference refresh, or
append-only evidence flow.
