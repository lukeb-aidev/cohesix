<file name=src/cuda/runtime.rs>
return Ok(GpuTelemetry {
    cuda_present: true,
    driver_version: version,
    mem_total: total as u64,
    mem_free: free as u64,
    exec_time_ns: self.last_exec_ns,
    gpu_utilization: util,
});
</file>

<file name=tests/test_cuda_presence.rs>
assert_eq!(telem.unwrap().cuda_present, rt.is_present());
</file>
