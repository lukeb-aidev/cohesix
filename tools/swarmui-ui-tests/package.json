{
  "name": "swarmui-ui-tests",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright UI-only regression tests for SwarmUI (replay-first).",
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  }
}
