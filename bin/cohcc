#!/bin/sh
# CLASSIFICATION: COMMUNITY
# Filename: cohcc v0.1
# Author: Lukas Bower
# Date Modified: 2026-02-21
BIN="$(dirname "$0")/../target/release/cohcc"
if [ ! -x "$BIN" ]; then
    echo "cohcc binary missing. run 'cargo build --release'" >&2
    exit 1
fi
exec "$BIN" "$@"
