// CLASSIFICATION: COMMUNITY
// Filename: plan9.ns v0.8
// Author: Lukas Bower
// Date Modified: 2027-12-02

# Cohesix default namespace
bind -a /usr/coh/bin /bin
bind -a /usr /usr
bind -a /tmp /tmp
bind -a /srv /srv
bind -a /mnt /mnt
srv? cuda /srv/cuda
srv? telemetry /srv/telemetry
srv? cohrole /srv/cohrole
