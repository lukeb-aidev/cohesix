# Author: Lukas Bower
# Purpose: Configure the sidecar-bus crate for host-side field bus adapters.
# Copyright 2026 Lukas Bower
[package]
name = "sidecar-bus"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["std"]
std = []
modbus = []
dnp3 = []
tokio = ["std", "dep:tokio"]

[dependencies]
tokio = { version = "1.53.1", features = ["rt", "time"], optional = true }
