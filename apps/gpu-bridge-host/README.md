<!-- Author: Lukas Bower -->
# GPU Bridge Host Tools (Future)

Host-side bridge utilities live here as defined in `docs/ARCHITECTURE.md` §3 and
`docs/GPU_NODES.md`. These tools remain out of scope for Milestone 0 but the
layout is reserved to keep the repository aligned with `docs/REPO_LAYOUT.md`.
