[package]
name = "scenario_compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
cohesix = { path = "../../cohesix" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
clap = { version = "4", features = ["derive"] }

[features]
default = []
