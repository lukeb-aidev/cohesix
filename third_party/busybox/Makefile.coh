BUSYBOX_DIR := $(CURDIR)
BUILD_DIR := $(CURDIR)/_build
OUTPUT := $(CURDIR)/../../bin/cohbox

all:
	mkdir -p $(BUILD_DIR)
	cp .config.coh $(BUSYBOX_DIR)/.config
	make -C $(BUSYBOX_DIR) CC=coh_cc busybox
	cp $(BUSYBOX_DIR)/busybox $(OUTPUT)

clean:
	make -C $(BUSYBOX_DIR) clean
	rm -f $(OUTPUT)
